<div align="center">
  <img src="https://visitor-badge.laobi.icu/badge?page_id=RedZapdos123.RedZapdos123" />
</div>

## FOSS Pull Requests

### Merged Pull Requests

| # | Repository | PR | Title | Merged Date | Description |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | json-schema-org/json-schema-spec | [#1724](https://github.com/json-schema-org/json-schema-spec/pull/1724) | Fix specs-ietf cert bundle path | 2026-07-01 | This PR fixes the IETF build on current Fedora images by ensuring Ruby OpenSSL's default certificate file path exists in the build container before `kramdown-rfc` runs. Closes #1723 `git diff --check`. `npm run build-ietf` |
| 2 | plotly/plotly.rs | [#402](https://github.com/plotly/plotly.rs/pull/402) | fix: allow plotly_static to compile without driver features | 2026-07-01 | Issue #400 reported that fresh workspace builds fail because `plotly_static` requires either `chromedriver` or `geckodriver` at compile time, even when static export is not being used. This PR changes `plotly_static` so that it compiles cleanly without a selected driver feature. |
| 3 | json-schema-org/json-schema-spec | [#1720](https://github.com/json-schema-org/json-schema-spec/pull/1720) | Fix README typo in authoring section | 2026-06-30 | This PR fixes a small typo in the authoring section of the README. |
| 4 | json-schema-org/json-schema-spec | [#1721](https://github.com/json-schema-org/json-schema-spec/pull/1721) | Use an IRI consistently in core spec | 2026-06-30 | This PR updates 5 occurrences of `a IRI` to `an IRI` in the core spec for consistent wording. |
| 5 | json-schema-org/json-schema-spec | [#1722](https://github.com/json-schema-org/json-schema-spec/pull/1722) | Fix possessive its typos in docs | 2026-06-30 | This PR fixes 4 possessive `its` typos in proposal and ADR docs. |
| 6 | rage-rb/rage | [#338](https://github.com/rage-rb/rage/pull/338) | [CI] Paginate changelog workflow PR file checks | 2026-06-29 | GitHub paginates `pulls.listFiles` responses. The current changelog workflow only inspects the first response page, so a pull request with more than 30 changed files can update `CHANGELOG.md` on a later page and still fail the check. This PR switches the workflow to `github.paginate(github.rest.pulls.listFiles, ...)` so the changelog check sees all changed files before deciding whether `CHANGELOG.md` was updated. |
| 7 | kubeedge/website | [#850](https://github.com/kubeedge/website/pull/850) | fix: add alt text to About avatar | 2026-06-29 | * **Please check if the PR fulfills these requirements** The commit message follows our guidelines Tests for the changes have been added (for bug fixes / features) Docs have been added / updated (for bug fixes / features) **Which issue(s) this PR fixes**: Fixes #846 * **What kind of change does this PR introduce?** (Bug fix, feature, docs update, ...) Bug fix * **What is the current behavior?** (You can also link to an open issue here) The About section avatar image renders without an `alt` attribute. |
| 8 | json-schema-org/json-schema-spec | [#1715](https://github.com/json-schema-org/json-schema-spec/pull/1715) | Fix IRI formatting in core examples | 2026-06-29 | This PR fixes 2 list items in the schema identification examples so only the URL is formatted as code, matching the surrounding entries. `git diff --check`. `npx remark --no-stdout --frail specs/jsonschema-core.md`. |
| 9 | json-schema-org/json-schema-spec | [#1714](https://github.com/json-schema-org/json-schema-spec/pull/1714) | Fix editorial typos in docs and specs | 2026-06-29 | This PR fixes 5 small editorial typos in the contributor docs, process docs, core spec, and a related ADR. `git diff --check`. `npx remark --no-stdout --frail CONTRIBUTING.md PROCESS.md adr/2024-11-2-assertion-format.md specs/jsonschema-core.md specs/proposals/propertyDependencies-adr.md`. |
| 10 | scarpe-team/scarpe | [#582](https://github.com/scarpe-team/scarpe/pull/582) | Fix stale dynamicRubyCallback mock expectations in test/test_web_wrangler.rb | 2026-06-29 | Fixes stale mocked bind expectations in `test/test_web_wrangler.rb`. `Scarpe::Webview::WebWrangler#initialize` now binds `dynamicRubyCallback`, but the mocked webview helper still expected only the older bind list. That caused all tests in `TestWebWranglerMocked` to fail during setup with `MockExpectationError`. |
| 11 | scarpe-team/scarpe | [#583](https://github.com/scarpe-team/scarpe/pull/583) | Pass filename and line to ShoesSpec eval backtraces | 2026-06-29 | Issue #453 reported that ShoesSpec backtraces were less useful because the Scarpe and Niente ShoesSpec runners still used bare `eval(code)`, which reports `(eval):...` instead of the test file path. This PR updates both ShoesSpec runners to accept `filename` and `line`, and evaluates test code with `eval(code, nil, filename, line)`. |
| 12 | unionai-oss/pandera | [#2393](https://github.com/unionai-oss/pandera/pull/2393) | fix: preserve xarray regex DataVar schemas across validation | 2026-06-29 | Issue #2390 reports that `DataVar(regex=True)` works on the first xarray `DatasetSchema.validate()` call, but mutates the schema to the first concrete match. After that, later validations treat the first matched variable name as a literal required data variable. |
| 13 | unionai-oss/pandera | [#2394](https://github.com/unionai-oss/pandera/pull/2394) | fix: handle missing regex matches in pyspark models | 2026-06-29 | Issue #1799 reports that PySpark `DataFrameModel` validation crashes with `AttributeError: 'list' object has no attribute 'tolist'` when a required regex field does not match any dataframe columns. This PR fixes the PySpark column backend error path so it reports the available columns without calling a pandas-only `tolist()` method on `DataFrame.columns`. |
| 14 | kubeedge/website | [#847](https://github.com/kubeedge/website/pull/847) | fix: unnest Job Center header CTA | 2026-06-29 | * **Please check if the PR fulfills these requirements** The commit message follows our guidelines Tests for the changes have been added (for bug fixes / features) Docs have been added / updated (for bug fixes / features) **Which issue(s) this PR fixes**: Fixes #843 * **What kind of change does this PR introduce?** (Bug fix, feature, docs update, ...) Bug fix * **What is the current behavior?** (You can also link to an open issue here) The `POST A JOB` CTA in the Job Center header renders as a nested interactive element: ``. |
| 15 | kubeedge/website | [#840](https://github.com/kubeedge/website/pull/840) | fix: add keys to case studies list items | 2026-06-28 | * **Please check if the PR fulfills these requirements** The commit message follows our guidelines Tests for the changes have been added (for bug fixes / features) Docs have been added / updated (for bug fixes / features) **Which issue(s) this PR fixes**: Fixes #835 * **What kind of change does this PR introduce?** (Bug fix, feature, docs update, ...) Bug fix * **What is the current behavior?** (You can also link to an open issue here) The case studies page maps its `` items without a `key` prop, which triggers the React list-key warning in development mode. |
| 16 | kubeedge/website | [#841](https://github.com/kubeedge/website/pull/841) | fix: add keys to Job Center list items | 2026-06-28 | * **Please check if the PR fulfills these requirements** The commit message follows our guidelines Tests for the changes have been added (for bug fixes / features) Docs have been added / updated (for bug fixes / features) **Which issue(s) this PR fixes**: Fixes #836 * **What kind of change does this PR introduce?** (Bug fix, feature, docs update, ...) Bug fix * **What is the current behavior?** (You can also link to an open issue here) The Job Center page maps its `` items without a `key` prop, which triggers the React list-key warning in development mode. |
| 17 | kubeedge/website | [#842](https://github.com/kubeedge/website/pull/842) | fix: add keys to Recent News author links | 2026-06-27 | * **Please check if the PR fulfills these requirements** The commit message follows our guidelines Tests for the changes have been added (for bug fixes / features) Docs have been added / updated (for bug fixes / features) **Which issue(s) this PR fixes**: Fixes #837 * **What kind of change does this PR introduce?** (Bug fix, feature, docs update, ...) Bug fix * **What is the current behavior?** (You can also link to an open issue here) The Recent News author links are rendered from a mapped array without a `key` prop, which triggers the React list-key warning in development mode. |
| 18 | kubeedge/website | [#839](https://github.com/kubeedge/website/pull/839) | fix: use semantic links in case study cards | 2026-06-27 | * **Please check if the PR fulfills these requirements** The commit message follows our guidelines Tests for the changes have been added (for bug fixes / features) Docs have been added / updated (for bug fixes / features) **Which issue(s) this PR fixes**: Fixes #838 * **What kind of change does this PR introduce?** (Bug fix, feature, docs update, ...) Bug fix * **What is the current behavior?** (You can also link to an open issue here) The case study cards rely on a clickable `` and an `` without an `href`, so the title and CTA are not semantic links and are not keyboard-focusable. |
| 19 | rage-rb/rage | [#334](https://github.com/rage-rb/rage/pull/334) | [Deferred] Ignore missing temp files during async cleanup | 2026-06-23 | Issue #333 reported that deferred disk backend temp-file cleanup could run asynchronously after the file had already been removed, raising `Errno::ENOENT` in later reactor-based specs. This PR updates the deferred disk backend to route async storage cleanup through a shared helper that still closes the file handle and ignores only missing-file unlink errors. |
| 20 | unionai-oss/pandera | [#2382](https://github.com/unionai-oss/pandera/pull/2382) | fix(strategies): avoid strategy truthiness checks | 2026-06-23 | Issue #2307 reported that pandas strategy entry-points still used truthiness checks on Hypothesis strategy objects, which emits `HypothesisWarning` messages when a chained strategy is supplied. This PR replaces the three remaining `if strategy:` guards in `pandera/strategies/pandas_strategies.py` with explicit `is not None` checks. |
| 21 | unionai-oss/pandera | [#2385](https://github.com/unionai-oss/pandera/pull/2385) | ci: make codecov upload failures non-blocking | 2026-06-23 | Issue #2384 reported that CI matrix jobs can fail after tests pass when the Codecov upload step hits a transient network or DNS error. This PR sets `fail_ci_if_error: false` on the `codecov/codecov-action@v4` steps in `ci-tests.yml`. This keeps successful test jobs green when Codecov upload fails transiently, while preserving `codecov/patch` and `codecov/project` as the actual coverage signals and keeping the existing carryforward flag behavior intact. |
| 22 | unionai-oss/pandera | [#2370](https://github.com/unionai-oss/pandera/pull/2370) | Use Codecov carryforward flags for CI coverage uploads | 2026-06-19 | Issue #2368 reported that `codecov/project` can show misleading coverage drops when CI jobs fail before running tests and before uploading coverage. This PR adds Codecov carryforward flag configuration and tags CI coverage uploads with matrix-specific flags. |
| 23 | openSUSE/osem | [#3803](https://github.com/openSUSE/osem/pull/3803) | Fix: event commercial permissions in non-admin flow | 2026-06-18 | Issue #3143 reported that organizers could edit a proposal in the non-admin flow but hit `AccessDenied` when creating a commercial for that proposal. This PR updates `Ability` so organizers and CFP team members can manage event commercials for conferences where they already have proposal-management access in non-admin views. |
| 24 | openSUSE/open-build-service | [#19815](https://github.com/openSUSE/open-build-service/pull/19815) | Isolate reused search term in full_text_search_spec | 2026-06-18 | Issue #19814 reported intermittent `ThinkingSphinx::Search::StaleIdsException` in `spec/models/full_text_search_spec.rb` on the current `master` branch. The flaky `specifying classes` examples reused the literal search term `test` while `:thinking_sphinx` specs use `DatabaseCleaner` deletion. |
| 25 | unionai-oss/pandera | [#2354](https://github.com/unionai-oss/pandera/pull/2354) | fix(pandas): preserve drop_invalid_rows in schema IO | 2026-06-18 | Issues #1339 and #1357 reported that `drop_invalid_rows` was lost when pandas `DataFrameSchema` objects were serialized to JSON or YAML and then deserialized. This PR updates the pandas schema statistics and pandas IO serialization paths to preserve `drop_invalid_rows` for both `DataFrameSchema` objects and pandas schema components. |
| 26 | rage-rb/rage | [#330](https://github.com/rage-rb/rage/pull/330) | [API] Ignore If-Modified-Since when If-None-Match is present | 2026-06-17 | Issue #329 reported that Rage still evaluated `If-Modified-Since` when `If-None-Match` was present, so an older `If-Modified-Since` could force a `200 OK` even when `If-None-Match` already matched. This PR updates `Rage::Request#fresh?` to give `If-None-Match` precedence when it is present and only fall back to `If-Modified-Since` when no `If-None-Match` header is sent. |
| 27 | rubocop/rubocop | [#15292](https://github.com/rubocop/rubocop/pull/15292) | [Fix #15290] Fix a false negative for Lint/ToEnumArguments with a braced hash | 2026-06-17 | Issue #15290 reported that `Lint/ToEnumArguments` accepted a braced hash as if it were a keyword argument or keyword rest argument. In Ruby, `to_enum(__method__, { limit: limit })` passes a positional hash, so the enumerator re-invokes the method with the wrong argument shape and raises `ArgumentError`. |
| 28 | rubocop/rubocop | [#15293](https://github.com/rubocop/rubocop/pull/15293) | [Fix #15291] Fix an incorrect autocorrect for Lint/LiteralInInterpolation with quoted symbols in hashes | 2026-06-16 | Issue #15291 reported that `Lint/LiteralInInterpolation` could serialize quoted symbols incorrectly when autocorrecting interpolated hashes. The hash serializer only special-cased spaces and quote characters, so symbols like `:'foo-bar'` were rewritten as `:foo-bar`, which changed the resulting string. |
| 29 | rage-rb/rage | [#325](https://github.com/rage-rb/rage/pull/325) | [API] Use weak comparison for If-None-Match validation | 2026-06-15 | Issue #324 reported that `If-None-Match` validation used exact string comparison, so a strong request ETag like `"..."` did not match Rage's weak response ETag `W/"..."`. This PR updates `Rage::Request#etag_matches?` to use weak comparison for `If-None-Match` validation, while keeping wildcard handling unchanged. |
| 30 | rubocop/rubocop | [#15218](https://github.com/rubocop/rubocop/pull/15218) | Fix TODO autocorrect safety comments for local custom cops | 2026-06-15 | Issue #14364 reported that `.rubocop_todo.yml` could ignore local custom cop configuration when generating autocorrect safety comments. When a local custom cop was not present in RuboCop's default config, `DisabledConfigFormatter` only checked the default config and incorrectly labeled the cop as supporting safe autocorrection. |
| 31 | rubocop/rubocop | [#15253](https://github.com/rubocop/rubocop/pull/15253) | Stop autocorrecting direct safe navigation in Lint/NumberConversion | 2026-06-15 | Issue #15252 reported that `Lint/NumberConversion` could produce incorrect autocorrects for safe navigation calls. When code like `foo&.to_i` was autocorrected to `Integer(foo, 10)`, the nil-safe behavior of `&.` was lost and the corrected code could raise when the receiver was `nil`. |
| 32 | openSUSE/obs-landing | [#572](https://github.com/openSUSE/obs-landing/pull/572) | Fix foster collaboration series block links | 2026-06-15 | Issue #569 reported that the shared foster collaboration series block used relative links for several posts, so they resolved under the current post URL and returned `404`. One link also pointed to the wrong date path. This PR updates the shared foster collaboration series partial to use the correct root relative post URLs and fixes the incorrect date path. |
| 33 | openSUSE/obs-landing | [#571](https://github.com/openSUSE/obs-landing/pull/571) | Fix content moderation series block link | 2026-06-15 | Issue #568 reported that the shared content moderation series block linked to `/2024/08/27/content-moderation/`, but the existing post is published at `/2024/08/28/content-moderation/`. This PR updates the shared content moderation series partial to use the correct post URL. |
| 34 | rage-rb/rage | [#328](https://github.com/rage-rb/rage/pull/328) | [Cookies] Match array-based cookie domains case-insensitively | 2026-06-14 | Issue #327 reported that array-based cookie domains were matched with exact string equality, so uppercase request hosts did not match lowercase configured domains. This PR updates `Rage::Cookies#[]=` to match array-based cookie domains case-insensitively while still using the configured domain value in the `Set-Cookie` header. |
| 35 | unionai-oss/pandera | [#2353](https://github.com/unionai-oss/pandera/pull/2353) | fix(pandas): keep ArrowString resolution stable | 2026-06-14 | Issue #2351 reported that resolving another pyarrow-backed pandas dtype could change how `pd.ArrowDtype(pyarrow.string())` is resolved. This PR updates the pandas engine to resolve pyarrow-backed pandas dtypes without importing `pandera.engines.pyarrow_engine` in that path, so the pandas-engine `ArrowString` registration stays stable. |
| 36 | unionai-oss/pandera | [#2350](https://github.com/unionai-oss/pandera/pull/2350) | fix: handle mixed-timezone pandas datetime coercion | 2026-06-14 | Issue #1309 reported that pandas datetime coercion could fail when a column contained mixed timezones or mixed offsets. This PR updates the pandas DateTime engine to retry parsing with `utc=True` only for mixed-timezone inputs that would otherwise fail coercion or remain object-typed after `pd.to_datetime(...)`. |
| 37 | unionai-oss/pandera | [#2369](https://github.com/unionai-oss/pandera/pull/2369) | Harden CI dependency installation | 2026-06-14 | Issue #2367 reported that large CI matrix jobs can fail before tests run when dependency downloads from PyPI or files.pythonhosted.org are interrupted. This PR adds uv cache coverage to CI jobs that run nox, and retries dependency installation in CI through a small nox install helper. |
| 38 | unionai-oss/pandera | [#2348](https://github.com/unionai-oss/pandera/pull/2348) | docs(pydantic): clarify v2 string coercion semantics | 2026-06-10 | Issue #2054 reported that coercion looked different when validating a dataframe with `Config.dtype = PydanticModel(...)` compared with a direct pandera schema. On the latest `main`, the remaining failing case comes from Pydantic v2 semantics: row-model validation does not coerce numeric values to strings by default. |
| 39 | openSUSE/obs-landing | [#563](https://github.com/openSUSE/obs-landing/pull/563) | Fix inconsistent heading sizes in blog posts | 2026-06-10 | Issue #170 reported that heading sizes in blog posts were inconsistent, with `h2` rendering larger than `h1`. This PR adds the missing `h1` font size rule for `.ui.vertical.stripe`, keeping the heading scale consistent in blog posts and other stripe sections. |
| 40 | rage-rb/rage | [#321](https://github.com/rage-rb/rage/pull/321) | [Router] Fall back to SERVER_NAME for exact host constraints | 2026-06-09 | Issue #320 reported that exact router host constraints only derived the request host from `HTTP_HOST`. This PR updates the router constrainer to fall back to `SERVER_NAME` when `HTTP_HOST` is missing, while still stripping the port from `HTTP_HOST` when it is present. |
| 41 | joke2k/faker | [#2367](https://github.com/joke2k/faker/pull/2367) | feat(locale): add mr_IN person, address, and phone_number providers | 2026-06-08 | Adds Marathi locale support under `mr_IN` with these providers: - `person` - `address` - `phone_number` - `date_time` It also adds tests in existing suites: - `tests/providers/test_person.py` (`TestMrIN`) - `tests/providers/test_address.py` (`TestMrIn`) - `tests/providers/test_phone_number.py` (`TestMrIn`) - `tests/providers/test_date_time.py` (`TestMrIN`) `mr_IN` was missing as a dedicated locale. |
| 42 | rage-rb/rage | [#319](https://github.com/rage-rb/rage/pull/319) | [Cookies] Use request host fallback when resolving cookie domains | 2026-06-08 | Issue #318 reported that cookie domain resolution could depend on `HTTP_HOST` only. This PR updates `Rage::Cookies` to derive the request host through `Rack::Request`, so cookie domain resolution follows the same host fallback semantics as request host handling when `HTTP_HOST` is missing and `SERVER_NAME` is present. |
| 43 | openSUSE/obs-landing | [#562](https://github.com/openSUSE/obs-landing/pull/562) | Mention default appliance credentials on /download | 2026-06-08 | Issue #262 reported that the `/download` page did not mention the default appliance credentials for either the system root account or the local OBS admin account. This PR updates the appliance configuration copy on the download page to include both default credentials: `root / opensuse` for the system login and `Admin / opensuse` for the local OBS instance. |
| 44 | rage-rb/rage | [#314](https://github.com/rage-rb/rage/pull/314) | [API] Reject invalid status symbols in head and render | 2026-06-07 | Issue #313 reported that `head` and `render` could accept invalid status symbols and silently set the response status to `nil`. This PR updates `RageController::API` to validate symbol statuses through `Rack::Utils.status_code` before mutating controller state. |
| 45 | rubocop/rubocop | [#15251](https://github.com/rubocop/rubocop/pull/15251) | Fix incorrect autocorrects for Lint/LiteralInInterpolation | 2026-06-06 | Issue #15250 reported that `Lint/LiteralInInterpolation` could produce incorrect autocorrects for interpolated string literals. When the interpolated string literal contained embedded double quotes or escaped interpolation text, the cop inserted the literal value into the surrounding interpolated string without escaping characters that are special in the outer string context. |
| 46 | rage-rb/rage | [#312](https://github.com/rage-rb/rage/pull/312) | [Request] Treat IPv6 literals as non-domain hosts | 2026-06-04 | Issue #311 reported that `Rage::Request#domain` could treat IPv6 literals as domain names and could raise when the request fell back to a bare IPv6 `SERVER_NAME`. This PR updates `Rage::Request` to detect IP literals using `IPAddr`, so IPv6 hosts are treated the same way as IPv4 hosts during domain extraction. |
| 47 | rage-rb/rage | [#309](https://github.com/rage-rb/rage/pull/309) | [Cookies] Preserve array domain matching when request host includes a port | 2026-06-03 | Issue #308 reported that array-based cookie domain matching could fail when the request host included a port. This PR updates `Rage::Cookies` to strip the port from `HTTP_HOST` before resolving configured cookie domains. It also adds a regression test that covers array-based cookie domain matching when the request host includes a port. |
| 48 | plotly/plotly.rs | [#403](https://github.com/plotly/plotly.rs/pull/403) | ci: harden windows static export readiness | 2026-06-02 | A flaky `windows-latest` Chrome CI run timed out waiting for `document.readyState` during `plot::tests::image_to_svg_string` even though the page finished loading immediately afterward. This PR slightly hardens the Windows-only static export readiness path by widening the Windows-only `document.readyState` wait and fixing the `#plotly-html-element` timeout loop so the timeout path behaves as intended. |
| 49 | rage-rb/rage | [#307](https://github.com/rage-rb/rage/pull/307) | [Router] Normalize exact host constraints when host includes a port | 2026-06-02 | This fixes exact host constraint matching when the request host includes a port. The router currently derives the `host` constraint directly from `HTTP_HOST`, so a route constrained with `host: "google.com"` does not match requests like `Host: google.com:3000`. |
| 50 | joke2k/faker | [#2357](https://github.com/joke2k/faker/pull/2357) | Added pan() and gstin() generators to en_IN provider | 2026-06-01 | Adds `pan()` and `gstin()` generators to the `en_IN` SSN provider. - `pan()` now generates values matching `^[A-Z]{5}[0-9]{4}[A-Z]$`. - `gstin()` now generates values matching `^[0-9]{2}[A-Z]{5}[0-9]{4}[A-Z][0-9A-Z]Z[0-9A-Z]$`. |
| 51 | plotly/plotly.rs | [#392](https://github.com/plotly/plotly.rs/pull/392) | Type trace axis ids | 2026-06-01 | Issue [plotly/plotly.py#5583](https://github.com/plotly/plotly.py/issues/5583) points out that trace `x_axis` and `y_axis` setters are not self-explanatory because users have to discover values like `"x2"` and `"y3"` from examples. This PR keeps the existing string-based axis ids, but gives them named types: - `XAxisId` - `YAxisId` The trace fields now use those named axis-id string types, and the trace setters accept `impl Into` / `impl Into` so existing string inputs continue to work without introducing an enum or a hardcoded axis limit. |
| 52 | rage-rb/rage | [#305](https://github.com/rage-rb/rage/pull/305) | [API] Reject malformed or empty HTTP token authorization headers | 2026-06-01 | Issue #304 reported that malformed or empty HTTP token `Authorization` headers could still be accepted by `RageController::API#authenticate_with_http_token` because the helper only checked the `Bearer` / `Token` prefix and then sliced the header at fixed offsets. |
| 53 | plotly/plotly.rs | [#397](https://github.com/plotly/plotly.rs/pull/397) | ci: use preinstalled browser binaries | 2026-05-27 | Maintainer requested that the CI fixes be split out from #392 into a separate PR. This PR keeps only the CI/browser setup changes needed for the static export workflow: - add a `setup-chrome` step id in the clippy job so later steps can reliably consume action outputs. |
| 54 | joke2k/faker | [#2369](https://github.com/joke2k/faker/pull/2369) | fix(en_IN): avoid shared state mutation in pincode_in_state | 2026-05-22 | Fixes shared state mutation in the en_IN address provider by avoiding in-place updates in `pincode_in_state(..., include_union_territories=True)`. Adds regression test to ensure class-level `state_pincode` remains unchanged. `pincode_in_state` assigned `codes = self.state_pincode` and then called `codes.update(self.union_territories_pincode)` when union territories were included. |
| 55 | unionai-oss/pandera | [#2303](https://github.com/unionai-oss/pandera/pull/2303) | Fix mypy type hinting for Polars DataFrameModel field annotations | 2026-05-21 | Issue #2297 reported that mypy throws type hinting errors when using Polars DataFrameModel field names as strings with direct column type annotations. The runtime behavior was correct (field access returned strings), but mypy incorrectly inferred the type as the annotation type (like `float`) instead of `str`. |
| 56 | dlt-hub/dlt | [#3824](https://github.com/dlt-hub/dlt/pull/3824) | fix(clickhouse): correct merge delete temp table handling with replica-safe temp-table engines | 2026-05-04 | I noticed that delete temp tables were always created with MergeTree in ClickHouse merge delete-insert jobs. On replicated or shared MergeTree deployments, this can make delete key visibility inconsistent across replicas, so old rows may survive while new rows are inserted and duplicates appear. |
| 57 | unionai-oss/pandera | [#2304](https://github.com/unionai-oss/pandera/pull/2304) | docs: clarify required extra for data synthesis strategies | 2026-05-02 | Issue #1649 reports the confusion between `hypotheses` and `strategies` extras when using data synthesis. This PR updates the data synthesis documentation to explicitly state that data synthesis requires `pandera[strategies]`, and clarifies that `pandera[hypotheses]` is for pandera hypothesis checks and does not install the `hypothesis` library. |
| 58 | dlt-hub/dlt | [#3851](https://github.com/dlt-hub/dlt/pull/3851) | fix(clickhouse): correct metadata sort keys to avoid full scans (Co-authored) | 2026-04-28 | Continutation of https://github.com/dlt-hub/dlt/pull/3819 due to the `ci from fork` label not working as expected. Closes https://github.com/dlt-hub/dlt/issues/3806 |
| 59 | koaning/drawdata | [#36](https://github.com/koaning/drawdata/pull/36) | Fix ScatterWidget circle_brush initialization order in AnyWidget runtimes | 2026-04-23 | Issue #34 reported a frontend initialization failure in ScatterWidget in non Jupyter AnyWidget runtimes because class button initialization could touch circle_brush before it was initialized. This PR updates ScatterWidget initialization to avoid referencing circle_brush before declaration in both source and bundled frontend files. |
| 60 | joke2k/faker | [#2362](https://github.com/joke2k/faker/pull/2362) | Fix UnicodeEncodeError in CLI docs on non-UTF consoles | 2026-04-17 | Fixes a CLI crash when printing provider docs to terminals using non-UTF encodings (for example cp950 on Windows). `python -m faker` can raise `UnicodeEncodeError` while printing provider examples that include characters unsupported by the output encoding (such as emoji on cp950). |
| 61 | unionai-oss/pandera | [#2288](https://github.com/unionai-oss/pandera/pull/2288) | test(pyspark): add str_length regressions for issues #1311 and #1314 | 2026-04-14 | Issue #1311 and #1314 reported that PySpark str_length checks could fail with NotImplementedError in two user paths: DataFrameSchema with Check.str_length(...) and DataFrameModel with Field(str_length=...). This PR adds focused regression tests in the PySpark SQL suite for both paths and validates both passing and failing cases, including emitted check names. |
| 62 | unionai-oss/pandera | [#2286](https://github.com/unionai-oss/pandera/pull/2286) | tests(mypy): add regression coverage for polars Column Decimal/Struct typing | 2026-04-13 | Issue #2152 reported that pa.Column mypy typing should accept pl.Decimal, and that it also happens with pl.Struct. This PR adds a focused mypy regression module and test that verify pa.Column accepts both parametrized polars dtypes: - pl.Decimal(precision=38, scale=6) - pl.Struct({...}) The regression test runs mypy with the repository config ( ests/mypy/config/no_plugin.ini) and asserts successful type checking. |
| 63 | unionai-oss/pandera | [#2287](https://github.com/unionai-oss/pandera/pull/2287) | fix(pandas): export errors in pandera.pandas public API | 2026-04-13 | Issue #2098 reported that Pylance could flag pa.errors as private because errors was imported in pandera.pandas but not included in its public export list. This PR updates the pandas entrypoint to include errors in __all__, making pa.errors an explicitly exported public API symbol. |
| 64 | pola-rs/polars | [#27119](https://github.com/pola-rs/polars/pull/27119) | fix(python): Fix always-true filter conversion to Iceberg filter | 2026-04-13 | fix(python): Corrects the try_convert_pyarrow_predicate handling of scalar boolean predicates. Issue #27106: NotImplementedError when using scan_iceberg with is_in([]) filter. I noticed that scalar predicates like pa.compute.scalar(False) could remain raw Python bool values after conversion, and PyIceberg filter scanning expects a PyIceberg boolean expression object. |
| 65 | unionai-oss/pandera | [#2281](https://github.com/unionai-oss/pandera/pull/2281) | Fix applymap deprecation warning in pandas parser backend | 2026-04-12 | Issue #2280 reported that element-wise dataframe parser validation could emit a pandas FutureWarning because DataFrame.applymap is deprecated. This PR updates the pandas parser backend to use DataFrame.map in the element-wise table parser path, removing deprecated applymap usage. |
| 66 | unionai-oss/pandera | [#2279](https://github.com/unionai-oss/pandera/pull/2279) | Fix DataFrame.from_records parser execution with coerce=True | 2026-04-12 | Issue #2129 reported that DataFrameModel parsers could be skipped in DataFrame.from_records when coerce=True was set on a field. This PR updates DataFrame.from_records to preserve the DataFrameModel generic parameter during construction, so model parser hooks are executed in the constructor validation path. |
| 67 | unionai-oss/pandera | [#2268](https://github.com/unionai-oss/pandera/pull/2268) | Fix JSON serialization for tuple-labeled schema columns | 2026-04-10 | Issue #2182 reported that schemas with tuple-labeled (MultiIndex-style) columns fail JSON serialization because tuple keys are not valid JSON object keys. This PR updates pandas schema JSON IO to keep the existing dict format for string column names, and serialize non-string column names with a JSON-safe list representation that roundtrips back to the original labels. |
| 68 | unionai-oss/pandera | [#2269](https://github.com/unionai-oss/pandera/pull/2269) | fix(ibis): preserve failed_cases row index positions | 2026-04-09 | Issue #2188 reported that Ibis table validation could produce failed_cases index values that did not match original row positions. This PR updates Ibis error formatting to derive failure row positions from check_output and apply them to materialized failure cases, preserving original row index positions without changing CheckResult.failure_cases shape. |
| 69 | unionai-oss/pandera | [#2270](https://github.com/unionai-oss/pandera/pull/2270) | fix(pandas): preserve DataFrameModel Index metadata in generated schema components | 2026-04-09 | Issue #2215 reported that metadata on DataFrameModel index fields was not persisted when schema components were built. This PR updates index field schema-property construction to pass through Field metadata, aligning Index behavior with Series/Column behavior. |
| 70 | unionai-oss/pandera | [#2260](https://github.com/unionai-oss/pandera/pull/2260) | fix(pandas): handle all-NaT nullable Date defaults in added columns | 2026-04-08 | The issue #2183 reported that the nullable pa.Date fields could fail validation when add_missing_columns=True auto-added a missing column with default=pd.NaT. This PR updates the pandas Date coercion path to preserve object/date semantics for all-NaT inputs, so nullable date columns added with NaT defaults pass dtype validation. |
| 71 | unionai-oss/pandera | [#2259](https://github.com/unionai-oss/pandera/pull/2259) | fix(pandas): preserve nulls for nullable typed model fields with coercion | 2026-04-08 | Issue #2176 reported that the inconsistent behavior for nullable bool and int typed columns in DataFrameModel with coerce=True. This PR updates the pandas DataFrameModel schema building path to normalize nullable bool/int typed fields to pandas nullable extension dtypes when coercion is enabled, preserving null values consistently. |
| 72 | unionai-oss/pandera | [#2261](https://github.com/unionai-oss/pandera/pull/2261) | fix(strategies): support exact_value in str_length generation | 2026-04-08 | This PR fixes the issue #2219 by updating the pandas string length strategy so it supports exact_value, while keeping the existing min/max behavior unchanged. It also adds a regression test that verifies DataFrameSchema.example() works with Check.str_length(exact_value=3) and produces strings of the expected fixed length. |
| 73 | tobymao/sqlglot | [#7463](https://github.com/tobymao/sqlglot/pull/7463) | fix(bigquery): parse MODEL-first AI/ML GENERATE signatures | 2026-04-07 | - add BigQuery parser hooks for MODEL-first GENERATE_* signatures (GENERATE_TEXT, GENERATE_TABLE, GENERATE_BOOL, GENERATE_INT, GENERATE_DOUBLE) - add generate-family expression types and SQL generation methods - add a BigQuery parser helper for generate signatures that supports table/query inputs and scalar prompt inputs - reparse dotted AI.* / ML.* calls in non-anonymous mode so custom function parsers are applied in expression contexts - add BigQuery dialect identity tests for the affected signatures Closes #7462. |
| 74 | tobymao/sqlglot | [#7457](https://github.com/tobymao/sqlglot/pull/7457) | fix(bigquery): support AI.FORECAST TVF parsing | 2026-04-06 | This PR adds BigQuery parser/generator support for `AI.FORECAST` table valued function syntax. The current `main` parses `ML.FORECAST(...)` but fails on documented `AI.FORECAST(...)` TVF forms with named arguments. Resolves #7456. |
| 75 | pydantic/pydantic-extra-types | [#381](https://github.com/pydantic/pydantic-extra-types/pull/381) | feat(json_schema): add JsonSchema type validated by jsonschema | 2026-04-05 | This PR adds a new `JsonSchema` type in `pydantic_extra_types.json_schema`. This type accepts either: - a JSON string, or - a `dict[str, Any]` It normalizes input to a dictionary and validates that dictionary as a JSON Schema using `jsonschema.Draft202012Validator.check_schema`. |
| 76 | pydantic/pydantic-extra-types | [#380](https://github.com/pydantic/pydantic-extra-types/pull/380) | feat(isin): add ISIN type with checksum validation | 2026-04-05 | This change adds ISIN validation support in pydantic-extra-types for issue #198. The issue came from missing first-class ISIN support, so users had to implement custom validators themselves. This PR introduces a new ISIN type in pydantic_extra_types.isin with strict format checks and Luhn checksum validation, normalizes lowercase input to uppercase, and adds tests for valid and invalid values plus JSON schema coverage. |
| 77 | pydantic/pydantic-extra-types | [#379](https://github.com/pydantic/pydantic-extra-types/pull/379) | fix(python): add missing Pendulum Interval wrapper support | 2026-04-05 | I noticed this happened because `pendulum_dt` already wraps `DateTime`, `Date`, `Time`, and `Duration`, but there was no wrapper for `Interval`. That made `Interval` unavailable as a first class validated type in this module. This PR adds the missing wrapper, validates existing `pendulum.Interval` values, parses ISO-8601 interval strings, and returns the project specific wrapper type. |
| 78 | unionai-oss/pandera | [#2258](https://github.com/unionai-oss/pandera/pull/2258) | fix(pandas): preserve tz-aware MultiIndex level metadata | 2026-04-04 | Description: Issue #2205 reported that timezone-aware MultiIndex levels could fail validation because level values were materialized with .values, which drops timezone metadata. This PR updates the pandas MultiIndex backend to avoid .values materialization in both full-materialization and optimized paths, preserving index-level dtype information. |
| 79 | unionai-oss/pandera | [#2256](https://github.com/unionai-oss/pandera/pull/2256) | Fix pyspark.pandas coerce+nullable failure case reporting | 2026-04-04 | This PR fixes issue #2150 for pandera.pandas used with pyspark.pandas. When coerce=True and ullable=True, invalid values in numeric coercion paths were being converted to nulls and not reported as coercion failure cases. - Detect coercion introduced nulls in pandas array backend for pyspark.pandas objects. |
| 80 | unionai-oss/pandera | [#2253](https://github.com/unionai-oss/pandera/pull/2253) | fix io serialization for custom check errors (#2143) | 2026-04-04 | - include custom check error messages in serialized check options for JSON/YAML - keep default built-in check errors omitted to avoid noisy schema output - add regression tests covering JSON and YAML round-trip behavior - pytest tests/io/test_pandas_io.py::test_check_custom_error_json_serialization_roundtrip -q - pytest tests/io/test_pandas_io.py::test_check_custom_error_yaml_serialization_roundtrip -q - pytest tests/io/test_pandas_io.py::test_to_yaml -q - pytest tests/io/test_pandas_io.py::test_io_json[single] -q I have reviewed all changes in this PR myself. |
| 81 | unionai-oss/pandera | [#2255](https://github.com/unionai-oss/pandera/pull/2255) | fix(mypy): type DataFrameModel field class attributes as str | 2026-04-03 | Description: test(python): Adds regression coverage for DataFrameModel class attribute column names when using bare annotations, including the pandera.typing.pandas module alias. Issue #2144 reports that class attributes on DataFrameModel are inferred as data dtypes (like int/float) instead of str unless wrapped in Series. |
| 82 | unionai-oss/pandera | [#2252](https://github.com/unionai-oss/pandera/pull/2252) | test pandas model optional union with typing alias (resolves issue: #2211) | 2026-04-03 | - test(python): Adds regression coverage for DataFrameModel optional column annotations using the pandera.typing.pandas module alias. - I noticed that issue #2211 requests support for PEP 604 union annotations to avoid Optional[...] style and UP007 lint conflicts. |
| 83 | unionai-oss/pandera | [#2251](https://github.com/unionai-oss/pandera/pull/2251) | Fix pandas typing empty-list constructor inference (#2247) | 2026-04-02 | - fix(python): Corrects DataFrame typing constructor handling of empty-list dict inputs. - I noticed that pandas infers float64 for empty-list columns, which caused schema validation to fail for typed columns. - This PR addresses it by adding a minimal pre-validation hook and a pandas-specific dtype alignment step for empty inferred float64 columns before schema validation. |


### Open Pull Requests

| # | Repository | PR | Title | Created Date | Description |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | rage-rb/rage | [#346](https://github.com/rage-rb/rage/pull/346) | [Specs][OpenAPI] Stabilize builder title specs across checkout names | 2026-07-02 | Issue #345 reported that the mocked OpenAPI builder specs hardcode `"title" => "Rage"` while the current default title is derived from `Rage.root.basename`. This PR keeps the current runtime behavior unchanged and stabilizes the mocked OpenAPI builder specs by stubbing `Rage.root` to a fixed `rage` path in the shared `mocked_rage_routes` test context. |
| 2 | json-schema-org/json-schema-spec | [#1728](https://github.com/json-schema-org/json-schema-spec/pull/1728) | Fix small documentation typos | 2026-07-02 | This PR fixes a few small documentation typos in the README and ADR text. |
| 3 | json-schema-org/json-schema-spec | [#1727](https://github.com/json-schema-org/json-schema-spec/pull/1727) | Add .dockerignore for IETF build | 2026-07-01 | This PR adds a root `.dockerignore` so the IETF build does not send local `node_modules`, generated output, and cache files in the Docker build context. On a fresh checkout after `npm ci`, `docker-compose build --progress plain` sent `113.76MB` of context. |
| 4 | json-schema-org/json-schema-spec | [#1726](https://github.com/json-schema-org/json-schema-spec/pull/1726) | Fix Dockerfile comment typo | 2026-07-01 | This PR fixes a typo in the Dockerfile comment for the Markdown to XML build step. |
| 5 | scarpe-team/scarpe | [#590](https://github.com/scarpe-team/scarpe/pull/590) | test: use temp export file for run_test_scarpe_app | 2026-06-30 | Issue #585 reported that `run_test_scarpe_app` in `test/test_helper.rb` wrote ShoesSpec export results to the fixed repo path `test/sspec.json`. This PR updates `run_test_scarpe_app` to allocate its export JSON with the existing tempfile helper, matching `run_scarpe_sspec` and isolating each test run from repo-local output files. |
| 6 | scarpe-team/scarpe | [#589](https://github.com/scarpe-team/scarpe/pull/589) | Refresh stale HTML fixtures for current renderer output | 2026-06-30 | Issue #588 reported that `bundle exec rake test:check_html_fixtures` fails on latest `main` because the checked in HTML fixtures no longer match the current renderer output. This PR regenerates `test/wv/html_fixtures` to match the current slot and drawable HTML output. |
| 7 | scarpe-team/scarpe | [#587](https://github.com/scarpe-team/scarpe/pull/587) | Make LoggedTest cleanup tolerate missing log files | 2026-06-30 | Issue #586 reported that `Scarpe::Test::LoggedTest` cleanup could raise `Errno::ENOENT` when a globbed test failure log disappeared before `File.unlink` ran. This PR updates the LoggedTest cleanup paths to use `FileUtils.rm_f` in both setup cleanup and `remove_unsaved_logs`, so missing files are treated as best-effort deletes. |
| 8 | zopencommunity/coreutilsport | [#115](https://github.com/zopencommunity/coreutilsport/pull/115) | Remove duplicate stdbuf entry in buildenv | 2026-06-29 | Removed the duplicate `stdbuf` entry from `ZOPEN_COREUTILS` in `buildenv`. This is a cleanup only and does not change the installed command set. |
| 9 | zopencommunity/coreutilsport | [#114](https://github.com/zopencommunity/coreutilsport/pull/114) | Add included utils list to README | 2026-06-29 | Document the subset of coreutils commands currently shipped by this port and note the default `g` command prefix used after install. The list mirrors the `ZOPEN_COREUTILS` allowlist in `buildenv`. Fixes #92. |
| 10 | kubeedge/website | [#849](https://github.com/kubeedge/website/pull/849) | fix: make Case Category filters keyboard accessible | 2026-06-28 | * **Please check if the PR fulfills these requirements** The commit message follows our guidelines Tests for the changes have been added (for bug fixes / features) Docs have been added / updated (for bug fixes / features) **Which issue(s) this PR fixes**: Fixes #845 * **What kind of change does this PR introduce?** (Bug fix, feature, docs update, ...) Bug fix * **What is the current behavior?** (You can also link to an open issue here) The `Case Category` filter chips render as clickable `div` elements, so they are not keyboard focusable and do not expose button semantics. |
| 11 | kubeedge/website | [#848](https://github.com/kubeedge/website/pull/848) | fix: unnest Case Studies header CTA | 2026-06-28 | * **Please check if the PR fulfills these requirements** The commit message follows our guidelines Tests for the changes have been added (for bug fixes / features) Docs have been added / updated (for bug fixes / features) **Which issue(s) this PR fixes**: Fixes #844 * **What kind of change does this PR introduce?** (Bug fix, feature, docs update, ...) Bug fix * **What is the current behavior?** (You can also link to an open issue here) The `POST YOUR CASE` CTA in the Case Studies header renders as a nested interactive element: ``. |
| 12 | scrapy/scrapy | [#7689](https://github.com/scrapy/scrapy/pull/7689) | Scope LogCounterHandler to the current crawler | 2026-06-27 | LogCounterHandler currently increments stats for any log record it receives. Filtering only records that already carry crawler or spider metadata is not enough, because some Scrapy owned log records are emitted without that metadata and can still be counted by the wrong crawler when multiple crawlers run in the same process. |
| 13 | openSUSE/open-build-service | [#19874](https://github.com/openSUSE/open-build-service/pull/19874) | Return unknown_repository for missing rebuild repository | 2026-06-24 | Issue #19873 reports that `POST /source//?cmd=rebuild` can raise `NoMethodError` when `repo` points to a repository that does not exist in the project. This PR updates `SourcePackageCommandController#rebuild` to use an existence check for the optional repository parameter, so the action returns `unknown_repository` instead of calling `empty?` on `nil`. |
| 14 | opiproject/opi-api | [#490](https://github.com/opiproject/opi-api/pull/490) | fix(storage): add missing VirtioScsi resource_reference types | 2026-06-24 | Issue #489 reported that two Virtio SCSI resource reference fields were missing explicit `google.api.resource_reference` typing. `VirtioScsiLun.target_name_ref` and `StatsVirtioScsiLunRequest.controller_name_ref` referenced other storage resources, but unlike nearby fields in the same proto they only declared field behavior. |
| 15 | opiproject/opi-api | [#488](https://github.com/opiproject/opi-api/pull/488) | docs(inventory): remove leaked TBD comments | 2026-06-23 | Issue #487 reported that placeholder public comments in `inventory/inventory.proto` were still set to `TBD`, and those placeholders were leaking into checked-in generated documentation and Swagger output. `MemoryInfo.total_usable_bytes` had no real field description, and `PCIeDeviceInfo` still carried unresolved `Type 9 or Type 41 ?` text. |
| 16 | opiproject/opi-spdk-bridge | [#893](https://github.com/opiproject/opi-spdk-bridge/pull/893) | fix(lint): resolve remaining goconst findings | 2026-06-23 | Issue #892 reported that latest `main` still had remaining `goconst` findings after the `golangci-lint` v2 migration. Most of the findings came from repeated table-driven test strings. The repository already excludes several noisy linters for `_test.go`, but `goconst` was still enabled there, which left 374 test-only findings. |
| 17 | opiproject/opi-spdk-bridge | [#891](https://github.com/opiproject/opi-spdk-bridge/pull/891) | test(delete): replace meaningless response checks | 2026-06-23 | Issue #515 reported that several `Delete*` tests only compared `reflect.TypeOf(response)` with `reflect.TypeOf(tt.out)`, which is meaningless for these gRPC delete methods because both sides are statically `*emptypb.Empty`. This PR replaces those dead checks with `proto.Equal(response, tt.out)`, so the tests assert the actual response value instead of only its static type. |
| 18 | kubeedge/website | [#833](https://github.com/kubeedge/website/pull/833) | fix: make Recent News links keyboard accessible | 2026-06-22 | * **Please check if the PR fulfills these requirements** The commit message follows our guidelines Tests for the changes have been added (for bug fixes / features) Docs have been added / updated (for bug fixes / features) **Which issue(s) this PR fixes**: `, or `Fixes (paste link of issue)`. |
| 19 | kubeedge/website | [#831](https://github.com/kubeedge/website/pull/831) | fix: make Job Center cards keyboard accessible | 2026-06-22 | * **Please check if the PR fulfills these requirements** The commit message follows our guidelines Tests for the changes have been added (for bug fixes / features) Docs have been added / updated (for bug fixes / features) **Which issue(s) this PR fixes**: `, or `Fixes (paste link of issue)`. |
| 20 | openSUSE/osem | [#3808](https://github.com/openSUSE/osem/pull/3808) | Test: stabilize proposal confirmation feature spec | 2026-06-18 | Issue #3807 reported that `spec/features/proposals_spec.rb` could fail intermittently when a participant confirms a proposal. This PR updates the proposal confirmation feature spec to avoid a stale Selenium node when the rails-ujs confirmation link redirects to the registration page. |
| 21 | rage-rb/rage | [#332](https://github.com/rage-rb/rage/pull/332) | [Request] Normalize malformed SERVER_NAME fallback hosts | 2026-06-18 | Issue #331 reported that host fallback paths could break when `HTTP_HOST` was missing and `SERVER_NAME` already included a port, because Rage used the malformed fallback value as-is. This PR normalizes malformed `SERVER_NAME` fallback hosts before they are used in request host handling, exact host constraints, and cookie domain resolution. |
| 22 | kubeedge/kubeedge | [#6885](https://github.com/kubeedge/kubeedge/pull/6885) | fix: reject unsupported tunnel message types | 2026-06-17 | Description: Issue #6882 reported that unsupported tunnel message types could panic `edgecore` when they reached `TunnelSession.ServeConnection()`. This PR keeps tunnel message parsing permissive and hardens handling at the application layer: - `pkg/stream/message.go` continues to parse and return tunnel messages even when the message type is unsupported. |
| 23 | kubeedge/kubeedge | [#6884](https://github.com/kubeedge/kubeedge/pull/6884) | fix: return CLOSE_CONNECT for MessageTypeCloseConnect | 2026-06-17 | Issue #6883 reported that `MessageTypeCloseConnect` stringified as `UNKNOWN` because it was missing from `MessageType.String()`. This PR updates `pkg/stream/message.go` to return `CLOSE_CONNECT` for `MessageTypeCloseConnect`. It also adds a regression test case in `pkg/stream/message_test.go` to cover the missing string mapping. |
| 24 | rubocop/rubocop | [#15299](https://github.com/rubocop/rubocop/pull/15299) | [Fix #15298] Fix hash string escaping in interpolated hashes | 2026-06-16 | Issue #15298 reported that `Lint/LiteralInInterpolation` could produce incorrect autocorrects for interpolated hashes when nested string keys or values contained escaped interpolation text or backslashes. This PR updates the hash string serialization path to reuse the existing escaping helper for string keys and values inside interpolated hashes, preserving the stringified hash output during autocorrect. |
| 25 | kubeedge/kubeedge | [#6863](https://github.com/kubeedge/kubeedge/pull/6863) | fix: add devicestatuses permission to cloudcore rbac | 2026-06-12 | Issue #6862 reported that `cloudcore` RBAC was missing permission for `devicestatuses.devices.kubeedge.io`, which caused repeated forbidden errors when the `DeviceStatus` informer started. This PR updates the cloudcore RBAC manifests to include `devicestatuses` in all affected manifest variants: - `build/cloud/03-clusterrole.yaml` - `build/cloud/ha/01-ha-prepare.yaml` - `manifests/charts/cloudcore/templates/rbac_cloudcore.yaml` The fix is minimal and limited to the affected RBAC resource lists. |
| 26 | pola-rs/polars | [#27895](https://github.com/pola-rs/polars/pull/27895) | fix(python): Infer int widths for list literals | 2026-06-07 | Issue #13114 reported that `pl.lit(1)` inferred `Int32`, while `pl.lit([1])` inferred `List(Int64)` because list literals were materialized through `Series([value])`, which widened inner integers to `Int64`. Issue #27894 reported the same widening for list literals with null-only or empty nested sibling lists, such as `[None, 1]`, `[[None], [1]]`, and `[[], [1]]`. |
| 27 | uyuni-project/uyuni | [#12078](https://github.com/uyuni-project/uyuni/pull/12078) | Fix duplicate remaining-length counter on custom key form | 2026-06-07 | This PR fixes the duplicated remaining-length counter shown under the Description field on the Custom System Info key creation page. The legacy textarea helper was not idempotent, so running the old JS initialization more than once appended another counter wrapper for the same textarea and rebound the handler again. |
| 28 | rubocop/rubocop | [#15258](https://github.com/rubocop/rubocop/pull/15258) | Fix a false negative for `Lint/ToEnumArguments` with extra keyword args | 2026-06-07 | Issue #15257 reported that `Lint/ToEnumArguments` could miss extra keyword arguments. The cop verified that required and optional keyword parameters had matching arguments, but it did not reject additional explicit keyword pairs. As a result, code such as `def m(x:); to_enum(:m, x: x, y: 1); end` was accepted even though the enumerator re-invokes the method with an unexpected keyword argument and raises `ArgumentError` at runtime. |
| 29 | uyuni-project/uyuni-tools | [#801](https://github.com/uyuni-project/uyuni-tools/pull/801) | Fix interactive SQL logging in debug mode | 2026-06-01 | Issue #418 reported that `mgradm support sql -i --logLevel debug` could corrupt the interactive SQL output because debug logs were written to the same terminal used by `psql`. This PR updates the `mgradm` root logger initialization to disable console logging only for interactive `support sql` sessions when the log level is `debug` or `trace`, preserving file logging and keeping the existing console logging behavior for other commands. |
| 30 | openSUSE/open-build-service | [#19770](https://github.com/openSUSE/open-build-service/pull/19770) | Fix build repository status apidocs | 2026-05-31 | Issue #15889 reported that the API documentation was missing for: `GET /build/{project_name}/{repository_name}/{architecture_name}?view=status` This PR documents that behavior on the real existing endpoint: - `GET /build/{project_name}/{repository_name}/{architecture_name}` - without `view`, the endpoint returns the package list - with `view=status`, the endpoint returns the scheduler status for the repository architecture It also: - models both response shapes on the same endpoint with `oneOf` - adds a dedicated schema for the repository architecture status response - aligns the documented status examples with current backend behavior by runtime-sampling finished, building, scheduling, dirty, and detailed states - fixes Swagger UI XML example rendering for `oneOf` responses so the examples render without the broken placeholder output Closes #15889. |
| 31 | koaning/drawdata | [#35](https://github.com/koaning/drawdata/pull/35) | Fix BarWidget CSS leakage by scoping selectors | 2026-04-22 | Issue #26 reported that BarWidget CSS leaked into other notebook outputs because generic selectors such as .container and label were globally applied. This PR updates BarWidget styling, and control selection to use widget scoped classes in both source, and bundled JS paths, preserving styling isolation across notebook outputs. |
| 32 | koaning/scikit-lego | [#784](https://github.com/koaning/scikit-lego/pull/784) | fix: Add `date_series` as `TimeGapSplit` parameter and deprecate `date_serie` | 2026-04-16 | Issue #761 reported a typo in the `TimeGapSplit` constructor argument name, where `date_serie` was used instead of `date_series`. This PR adds a backward compatible fix: - Supports the correct argument name `date_series` - Keeps support for the legacy name `date_serie` - Validates that exactly one of `date_series` or `date_serie` is provided - Keeps internal compatibility by aliasing `date_serie` to `date_series` - Updates `TimeGapSplit` docs examples to use `date_series` It also adds regression tests that cover: - `date_series` works correctly - Passing both `date_series` and `date_serie` raises a clear `ValueError` Closes #761. |
| 33 | fb55/css-select | [#1771](https://github.com/fb55/css-select/pull/1771) | feat: add :focus-visible and :focus-within support | 2026-04-05 | - Add :focus-visible support via an optional adapter isFocusVisible method. - Add :focus-within support via an optional adapter isFocused method. - Extend adapter typings for isFocused and isFocusVisible. |
| 34 | fb55/css-select | [#1770](https://github.com/fb55/css-select/pull/1770) | Use adapter equality for multi-context :scope | 2026-04-05 | - Make :scope use adapter equality consistently for multiple context nodes. - Replace reference-based context.includes(element) with context.some(node => equals(node, element)). - Add regression test covering multiple context elements with a custom adapter equals implementation. |
| 35 | fb55/css-select | [#1769](https://github.com/fb55/css-select/pull/1769) | Validate pseudo-class argument arity consistently | 2026-04-05 | - Add explicit argument validation for subselect pseudo-classes when called without required arguments. - Add argument validation for filter pseudo-classes that require an argument. - Reject arguments for filter pseudo-classes that do not accept them. |
| 36 | fb55/css-select | [#1765](https://github.com/fb55/css-select/pull/1765) | Add support for :host pseudo-class | 2026-04-05 | - Add support for :host by aliasing it to :scope. - Add regression tests that verify :host matches the root element and context elements. - Document :host support in the README selector list. |
| 37 | ibis-project/ibis | [#11988](https://github.com/ibis-project/ibis/pull/11988) | fix(bigquery): preserve resolved database in create_view return path | 2026-04-04 | This PR fixes BigQuery `create_view` return-path handling for resolved database locations as reported in #11974. `create_view` already resolves `database` into `catalog` and `db` for the generated SQL, but the returned `self.table(...)` lookup could still diverge: - for `project.dataset` inputs it reused the original string instead of the parsed tuple - for omitted `database` it could return the unresolved `db=None` path instead of the current dataset used in the SQL target This change reuses the effective `catalog` and `db` values for both the `CREATE VIEW` statement and the returned table expression. |
| 38 | duckdb/duckdb | [#21821](https://github.com/duckdb/duckdb/pull/21821) | Add regression tests for CREATE SEQUENCE option order | 2026-04-03 | - add regression coverage for `CREATE SEQUENCE` option-order handling - cover both ascending `MINVALUE ... INCREMENT 1` and descending `MAXVALUE ... INCREMENT -1` Issue #21813 reproduces on DuckDB 1.5.1, where an explicit `MINVALUE` can be overwritten when `INCREMENT` is parsed later in the option list. |
| 39 | ibis-project/ibis | [#11984](https://github.com/ibis-project/ibis/pull/11984) | fix(sql): preserve timezone-cast semantics in trino and duckdb | 2026-03-31 | This PR fixes timezone cast regressions in DuckDB and Trino. For DuckDB, timezone aware extraction from casted UTC timestamp columns now matches scalar casts, and timezone-aware string/date casts preserve the intended instant for `epoch_seconds()`. For Trino, casts to timezone aware timestamps now distinguish between two cases: - string/date and naive timestamp inputs are localized with `WITH_TIMEZONE` - timezone-aware timestamp inputs are converted with `AT_TIMEZONE` This fixes the incorrect `epoch_seconds()` behavior for timezone-aware casts from string/date inputs and preserves the expected `hour()` and `time()` behavior when casting UTC timestamps to another timezone. |
| 40 | JDAI-CV/fast-reid | [#757](https://github.com/JDAI-CV/fast-reid/pull/757) | Add detailed developer documentation and contribution guides. | 2025-10-08 | This PR significantly enhances FastReID's documentation and developer experience by adding deatiled guides for contributors, developers, and users. This makes the project more accessible to new contributors and provides clear guidance for extending FastReID. |
| 41 | JDAI-CV/fast-reid | [#756](https://github.com/JDAI-CV/fast-reid/pull/756) | Add detailed model zoo compatibility system with automatic config generation. | 2025-10-08 | This PR introduces a detailed model zoo compatibility system that solves the major issue of missing or incompatible configuration files for pretrained models. Users can now automatically generate compatible configurations for any model from the FastReID model zoo. |
| 42 | JDAI-CV/fast-reid | [#755](https://github.com/JDAI-CV/fast-reid/pull/755) | Modernize Python packaging with pyproject.toml and enhanced compatibility. | 2025-10-08 | This PR modernizes FastReID's packaging system by migrating from traditional setup.py to modern pyproject.toml, bringing the project up to current Python packaging standards and improving installation reliability across different environments. The changes include a complete migration to PEP 517/518 standards with pyproject.toml. |


## FOSS Issues Authored

### Open Issues

| # | Repository | Issue | Title | Created Date | Description |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | ibis-project/ibis | [#12033](https://github.com/ibis-project/ibis/issues/12033) | bug: PostgreSQL CI image build fails because clang19 and llvm19 are unavailable in postgis/postgis:18-3.6-alpine | 2026-07-02 | The PostgreSQL jobs in the Backends workflow are failing before any backend tests start, during service startup, while building the `postgres` image from `docker/postgres/Dockerfile`. I observed the same failure in all of these places: - PR run: https://github.com/ibis-project/ibis/actions/runs/28573540241/job/84867447200 - `main` run: https://github.com/ibis-project/ibis/actions/runs/28120288053/job/83270194575 - Renovate PR run: https://github.com/ibis-project/ibis/actions/runs/28304057447/job/83857170892 I can also reproduce it locally with: - `docker build --progress=plain -f docker/postgres/Dockerfile docker/postgres` That fails at: - `RUN apk add --no-cache git build-base clang19 llvm19` I also checked the current `postgis/postgis:18-3.6-alpine` image locally. |
| 2 | rage-rb/rage | [#345](https://github.com/rage-rb/rage/issues/345) | [Specs][OpenAPI] Builder specs assume checkout directory basename is `rage` | 2026-07-02 | The OpenAPI builder specs assume the checkout directory basename is `rage` and hardcode `"title" => "Rage"` in expected output. At the same time, the actual default OpenAPI title is built from `Rage.root.basename` in `lib/rage/openapi/converter.rb`. That means the mocked OpenAPI builder specs become checkout-directory-dependent. |
| 3 | scarpe-team/scarpe | [#588](https://github.com/scarpe-team/scarpe/issues/588) | test:check_html_fixtures fails on latest main because HTML fixtures are stale | 2026-06-30 | On latest `main`, `bundle exec rake test:check_html_fixtures` fails because the checked-in HTML fixtures no longer match the current renderer output. The generated HTML now includes slot wrapper changes such as `position:relative` on slot containers and `display:contents` inner wrappers, so many fixtures in `test/wv/html_fixtures` are stale. |
| 4 | scarpe-team/scarpe | [#586](https://github.com/scarpe-team/scarpe/issues/586) | Make LoggedTest cleanup tolerate missing log files in unit_test_helpers.rb | 2026-06-30 | On latest `main`, `Scarpe::Test::LoggedTest` still uses raw `File.unlink` cleanup in `scarpe-components/lib/scarpe/components/unit_test_helpers.rb`: ```ruby Dir["#{log_dir}/test_failure*.log"].each { |fn| File.unlink(fn) } ``` and: ```ruby File.unlink(f) ``` If a matched log file is already gone by the time unlink runs, cleanup raises `Errno::ENOENT` instead of tolerating the missing file. |
| 5 | scarpe-team/scarpe | [#585](https://github.com/scarpe-team/scarpe/issues/585) | Replace fixed test/sspec.json export path with temp file in test/test_helper.rb | 2026-06-30 | On latest `main`, `run_test_scarpe_app` in `test/test_helper.rb` still writes ShoesSpec export results to a fixed repo path: ```ruby test_output = File.expand_path(File.join __dir__, "sspec.json") ``` Unlike `run_scarpe_sspec`, it does not allocate a per-run temp file for the export JSON. |
| 6 | kubeedge/website | [#845](https://github.com/kubeedge/website/issues/845) | BUG: Case Category filter chips are not keyboard accessible | 2026-06-28 | **Describe the bug** The `Case Category` filter chips on the Case Studies page are clickable `div` elements with `onClick`, but they are not keyboard focusable and do not expose button semantics. As a result, keyboard users cannot operate the filters. **To Reproduce** 1. |
| 7 | kubeedge/website | [#844](https://github.com/kubeedge/website/issues/844) | BUG: Case Studies header CTA nests a link inside a button | 2026-06-28 | **Describe the bug** The `POST YOUR CASE` CTA in the Case Studies header nests an anchor inside a button. That creates invalid interactive nesting (`...`), which is poor HTML semantics and can cause inconsistent keyboard or assistive-technology behavior. |
| 8 | openSUSE/open-build-service | [#19873](https://github.com/openSUSE/open-build-service/issues/19873) | rebuild with unknown repository raises NoMethodError instead of unknown_repository | 2026-06-24 | `SourcePackageCommandController#rebuild` raises `NoMethodError` when `repo` points to a repository that does not exist in the project. On current `master`, the controller checks the repository with: ```ruby @project.repositories.find_by(name: params[:repo]).empty? |
| 9 | opiproject/opi-api | [#489](https://github.com/opiproject/opi-api/issues/489) | storage/virtio-scsi: add missing resource_reference types for target_name_ref and controller_name_ref | 2026-06-23 | While checking the broader resource reference annotation gaps tracked in #379, I found two additional straightforward misses in `storage/frontend_virtio_scsi.proto`. These fields represent other resources, but they do not currently declare `(google.api.resource_reference).type`: - `storage/frontend_virtio_scsi.proto:190` - `VirtioScsiLun.target_name_ref` - `storage/frontend_virtio_scsi.proto:421` - `StatsVirtioScsiLunRequest.controller_name_ref` The same file already uses explicit resource reference typing for related fields, for example: - `CreateVirtioScsiLunRequest.parent` uses `child_type = "opi_api.storage.v1/VirtioScsiTarget"` - `DeleteVirtioScsiLunRequest.name` uses `type = "opi_api.storage.v1/VirtioScsiLun"` - `CreateVirtioScsiTargetRequest.parent` uses `child_type = "opi_api.storage.v1/VirtioScsiController"` So these two fields stand out as inconsistent with the rest of the schema and with AIP-122 guidance for fields that reference another resource. |
| 10 | opiproject/opi-api | [#487](https://github.com/opiproject/opi-api/issues/487) | docs(inventory): remove TBD placeholder comments leaking into generated docs and swagger | 2026-06-23 | Latest `main` still contains placeholder public comments in `inventory/inventory.proto`, and those placeholders are propagated into generated documentation and OpenAPI output. On latest `main`, `inventory/inventory.proto` still has unresolved placeholder comments: - `inventory/inventory.proto:167` - `// TBD` - `inventory/inventory.proto:172` - `// TBD: Type 9 or Type 41 ?` Because these comments are public proto comments, they leak into generated artifacts that are checked into the repository: - `inventory/v1/autogen.md:160` - `total_usable_bytes` description is `TBD` - `inventory/v1/autogen.md:171` - `PCIeDeviceInfo` section still says `TBD: Type 9 or Type 41 ?` - `inventory/v1/gen/go/inventory.swagger.json:216` - `total_usable_bytes` has `"title": "TBD"` - `inventory/v1/gen/go/inventory.swagger.json:257` - `PCIeDeviceInfo` description still contains `TBD: Type 9 or Type 41 ?` - Generated Go comments also inherit same placeholders: - `inventory/v1/gen/go/inventory.pb.go:516` - `inventory/v1/gen/go/inventory.pb.go:568` - `v1/gen/go/inventory/inventory.pb.go:498` - `v1/gen/go/inventory/inventory.pb.go:550` Public inventory API comments should use final descriptive wording, not unresolved placeholders, and regenerated docs/swagger should no longer expose `TBD`. |
| 11 | opiproject/opi-spdk-bridge | [#892](https://github.com/opiproject/opi-spdk-bridge/issues/892) | lint: goconst findings remain on latest main after v2 migration | 2026-06-23 | After the repository migrated to `golangci-lint` v2, latest `main` still has real `goconst` findings in `pkg/backend`, `pkg/frontend`, and `pkg/middleend`. This is separate from [#888](https://github.com/opiproject/opi-spdk-bridge/issues/888), which fixed the v2 config format itself. |
| 12 | kubeedge/website | [#832](https://github.com/kubeedge/website/issues/832) | BUG: Homepage Recent News blog posts are not keyboard accessible | 2026-06-22 | **Describe the bug** On the homepage `Recent News` section, blog post entries are mouse-only because each post title is rendered as a clickable `` instead of a semantic link/button. Keyboard users cannot focus or open the post titles. The `View All` control in the same section is also rendered as an `` with `onClick` but no `href`, so it is not exposed with proper link semantics and is skipped during keyboard navigation. |
| 13 | kubeedge/website | [#830](https://github.com/kubeedge/website/issues/830) | BUG: Job Center cards are not keyboard accessible | 2026-06-22 | **Describe the bug** On the Job Center page, each job card is clickable only with a mouse because the card root is a `div` with an `onClick` handler instead of a semantic link/button. The card does not receive keyboard focus, so keyboard users cannot open a job detail page. |
| 14 | openSUSE/osem | [#3807](https://github.com/openSUSE/osem/issues/3807) | Intermittent stale-node failure in proposal confirmation feature spec | 2026-06-18 | `spec:features` can fail intermittently in `spec/features/proposals_spec.rb` when the participant confirms a proposal. I hit this in GitHub Actions run `27712877168` on June 18, 2026: - Run: https://github.com/openSUSE/osem/actions/runs/27712877168 - Failing example: `rspec ./spec/features/proposals_spec.rb:162 # Event as a participant confirms a proposal` - Error: `Selenium::WebDriver::Error::UnknownError: unknown error: unhandled inspector error: {"code":-32000,"message":"Node with given id does not belong to the document"}` The failure happens while Selenium clicks the `confirm_proposal_*` rails-ujs link. |
| 15 | rage-rb/rage | [#331](https://github.com/rage-rb/rage/issues/331) | [Request] Host fallback paths do not normalize malformed SERVER_NAME values with an embedded port | 2026-06-17 | When `HTTP_HOST` is missing and `SERVER_NAME` already contains `:port`, Rage does not normalize the fallback host value before using it in request-host code paths. This is an edge case Rack env, but it currently breaks multiple host-based paths on latest `main`. |
| 16 | kubeedge/kubeedge | [#6883](https://github.com/kubeedge/kubeedge/issues/6883) | pkg/stream: MessageTypeCloseConnect stringifies as UNKNOWN | 2026-06-17 | **What happened**: On latest `master`, `stream.MessageTypeCloseConnect.String()` returns `UNKNOWN`. `pkg/stream/constants.go` defines `MessageTypeCloseConnect`, but `pkg/stream/message.go` does not handle it in `MessageType.String()`. This makes logs and debug output for close-connect messages misleading. |
| 17 | kubeedge/kubeedge | [#6882](https://github.com/kubeedge/kubeedge/issues/6882) | edgecore: unknown tunnel message type can panic TunnelSession | 2026-06-17 | **What happened**: On latest `master`, `edgecore` can panic on an unsupported tunnel message type. `pkg/stream.ReadMessageFromTunnel()` decodes the message type directly from tunnel bytes into `stream.MessageType` without validating that it is one of the known enum values. |
| 18 | rubocop/rubocop | [#15298](https://github.com/rubocop/rubocop/issues/15298) | Lint/LiteralInInterpolation incorrect autocorrect for escaped string content in interpolated hashes | 2026-06-16 | I have searched the existing issues and this hasn't been reported yet. I am running the latest stable version of RuboCop. Incorrect autocorrect `Lint/LiteralInInterpolation` I reproduced this on the latest stable RuboCop release and also on current `master` at `1f83e39bc2963f7d54fd8eebe6f28c49a94de0c4`. |
| 19 | kubeedge/kubeedge | [#6862](https://github.com/kubeedge/kubeedge/issues/6862) | cloudcore RBAC misses devicestatuses.devices.kubeedge.io permission | 2026-06-12 | **What happened**: On latest `master`, `cloudcore` repeatedly logs that it cannot list/watch `devicestatuses.devices.kubeedge.io`. ```text failed to list *v1beta1.DeviceStatus: devicestatuses.devices.kubeedge.io is forbidden: User "system:serviceaccount:kubeedge:cloudcore" cannot list resource "devicestatuses" in API group "devices.kubeedge.io" at the cluster scope ``` Also: ```bash kubectl auth can-i --as=system:serviceaccount:kubeedge:cloudcore list devicestatuses.devices.kubeedge.io ``` **What you expected to happen**: `cloudcore` should have RBAC to list/watch `devicestatuses.devices.kubeedge.io`, and should not emit repeated forbidden errors for the `DeviceStatus` informer. |
| 20 | pola-rs/polars | [#27894](https://github.com/pola-rs/polars/issues/27894) | pl.lit integer list literals with null-only or empty nested siblings infer Int64 | 2026-06-07 | I have checked that this issue has not already been reported. I have confirmed this bug exists on the [latest version](https://pypi.org/project/polars/) of Polars. ```python import polars as pl print(pl.select(a=[None, 1])) print(pl.select(a=[[None], [1]])) print(pl.select(a=[[], [1]])) ``` Actual output on `polars==1.41.2`: ```python shape: (1, 1) +-----------+ | a | | --- | | list[i64] | +===========+ | [null, 1] | +-----------+ shape: (1, 1) +------------------+ | a | | --- | | list[list[i64]] | +==================+ | [[null], [1]] | +------------------+ shape: (1, 1) +------------------+ | a | | --- | | list[list[i64]] | +==================+ | [[], [1]] | +------------------+ ``` _No response_ Issue #13114 covers `pl.lit(1)` vs `pl.lit([1])`. |
| 21 | rubocop/rubocop | [#15257](https://github.com/rubocop/rubocop/issues/15257) | Lint/ToEnumArguments false negative for extra keyword arguments | 2026-06-07 | I have searched the existing issues and this hasn't been reported yet. I am running the latest stable version of RuboCop. False negative Lint/ToEnumArguments Create a file with this content: ```rb def foo(x:) to_enum(__callee__, x: x, y: 1) end foo(x: 1).each {} ``` Run this command: ```sh rubocop --only Lint/ToEnumArguments test.rb ``` RuboCop reports no offense. |
| 22 | koaning/scikit-lego | [#785](https://github.com/koaning/scikit-lego/issues/785) | test_custom_shrinkage fails on the clean main due to strict float equality | 2026-04-16 | `tests/test_meta/test_grouped_predictor.py::test_custom_shrinkage` fails on a clean clone of `main` due to strict equality comparison between floating point values that differ only at machine precision. - Environment: WSL (Linux), Python 3.11.15, pytest 9.0.3 Run only the failing test: ```bash pytest -q tests/test_meta/test_grouped_predictor.py::test_custom_shrinkage -vv ``` Failing assertion (from clean run): ```text E AssertionError: assert [1.8333333333...6666666666665] == [1.8333333333...6666666666665] E At index 0 diff: 1.833333333333333 != 1.8333333333333333 ``` The test location is: - `tests/test_meta/test_grouped_predictor.py` line ~370 - Current assertion: `assert expected_prediction == shrink_est.predict(X).tolist()` The test should pass when predictions are numerically equivalent within floating point tolerance. |
| 23 | fb55/css-select | [#1768](https://github.com/fb55/css-select/issues/1768) | Missing support for :focus-within and :focus-visible | 2026-04-05 | Feature request: add support for `:focus-within` and `:focus-visible`. Current behavior: - Both selectors throw `Unknown pseudo-class`. Context: - css-select already supports dynamic-state pseudos like `:hover`, `:active`, and `:visited` via optional adapter methods. |
| 24 | fb55/css-select | [#1767](https://github.com/fb55/css-select/issues/1767) | :scope with multiple context nodes bypasses adapter equals and uses reference includes | 2026-04-05 | `:scope` appears to use different equality semantics depending on context length. Current behavior: - With a single context node, matching uses adapter `equals`. - With multiple context nodes, matching uses `context.includes(element)` (reference equality). |
| 25 | fb55/css-select | [#1766](https://github.com/fb55/css-select/issues/1766) | Pseudo-function argument validation inconsistencies causing null crashes or misleading errors | 2026-04-05 | Some pseudo-classes behave inconsistently when required arguments are missing. Examples: - `:lang` throws `Cannot read properties of null (reading 'split')` - `:icontains` throws `Cannot read properties of null (reading 'toLowerCase')` - `:nth-child` throws `Cannot read properties of null (reading 'match')` - `:has` / `:not` without arguments can report `Unknown pseudo-class` instead of a clear arity error Expected: - Invalid selectors should fail with consistent selector-validation errors (for example, "requires an argument"), not internal null/property errors or misleading unknown-pseudo errors. |
| 26 | joke2k/faker | [#2356](https://github.com/joke2k/faker/issues/2356) | Add pan() and gstin() generators to en_IN provider | 2026-04-03 | * Faker version: 40.12.0. * OS: Windows / Linux / macOS. Add `pan()` and `gstin()` generators to `en_IN`. |


### Closed Issues

| # | Repository | Issue | Title | Closed Date | Description |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | json-schema-org/json-schema-spec | [#1723](https://github.com/json-schema-org/json-schema-spec/issues/1723) | CI: specs-ietf fails on main due to kramdown-rfc cert-store check | 2026-07-01 | `specs-ietf` is failing on `main`, even for editorial only PRs. Examples: - https://github.com/json-schema-org/json-schema-spec/actions/runs/28474453171 - https://github.com/json-schema-org/json-schema-spec/actions/runs/26911430558 In both runs, `specs-markdown` passes and `specs-ietf` fails in `npm run build-ietf` with: ```text *** Configuration problem with OS certificate store. |
| 2 | plotly/plotly.rs | [#400](https://github.com/plotly/plotly.rs/issues/400) | Fresh workspace builds fail because plotly_static requires a driver feature | 2026-07-01 | When checking out the `plotly.rs` repository fresh and running `cargo check` or `cargo build` at the workspace root, the build fails immediately. The failure comes from the build script of the newly introduced `plotly_static` crate with the following error: ``` error: At least one of 'geckodriver' or 'chromedriver' features must be enabled. |
| 3 | rage-rb/rage | [#337](https://github.com/rage-rb/rage/issues/337) | [CI] Changelog workflow only checks the first page of PR files | 2026-06-29 | The changelog workflow checks pull request files with a single `github.rest.pulls.listFiles(...)` call and then only inspects that first response page. GitHub paginates pull request files. For PRs with more than 30 changed files, `CHANGELOG.md` can be present on a later page and the workflow can fail even though the changelog was updated. |
| 4 | kubeedge/website | [#846](https://github.com/kubeedge/website/issues/846) | BUG: About avatar image is missing alt text | 2026-06-29 | **Describe the bug** The About section avatar image is missing an `alt` attribute. This is an accessibility issue because assistive technologies do not get an alternative text description for the image. **To Reproduce** 1. |
| 5 | scarpe-team/scarpe | [#581](https://github.com/scarpe-team/scarpe/issues/581) | Fix stale dynamicRubyCallback mock expectations in test/test_web_wrangler.rb | 2026-06-29 | On latest `main`, the mocked WebWrangler tests in `test/test_web_wrangler.rb` still expect only three JS bindings during `Scarpe::Webview::WebWrangler` initialization: - `puts` - `scarpeAsyncEvalResult` - `scarpeHeartbeat` `WebWrangler` now also binds `dynamicRubyCallback`, so all tests in `TestWebWranglerMocked` fail during setup with `MockExpectationError`. |
| 6 | kubeedge/website | [#843](https://github.com/kubeedge/website/issues/843) | BUG: Job Center header CTA nests a link inside a button | 2026-06-29 | **Describe the bug** The `POST A JOB` CTA in the Job Center header nests an anchor inside a button. That creates invalid interactive nesting (`...`), which is poor HTML semantics and can cause inconsistent keyboard or assistive-technology behavior. **To Reproduce** 1. |
| 7 | kubeedge/website | [#835](https://github.com/kubeedge/website/issues/835) | BUG: Case studies list items are missing React keys | 2026-06-28 | **Describe the bug** The case studies list renders mapped `` items without a React `key` prop. In development mode, React logs a list-key warning for the `CaseStudies` render. **To Reproduce** Steps to reproduce the behavior: 1. |
| 8 | kubeedge/website | [#836](https://github.com/kubeedge/website/issues/836) | BUG: Job Center list items are missing React keys | 2026-06-28 | **Describe the bug** The job list on `/job-center` renders mapped `` items without a React `key` prop. In development mode, React logs a list-key warning for the `JobCenter` render. **To Reproduce** Steps to reproduce the behavior: 1. |
| 9 | kubeedge/website | [#837](https://github.com/kubeedge/website/issues/837) | BUG: Recent News author links are missing React keys | 2026-06-27 | **Describe the bug** The homepage Recent News section renders the author links from a mapped array without a React `key` prop. In development mode, React logs a list-key warning for the `BlogPost` render. **To Reproduce** Steps to reproduce the behavior: 1. |
| 10 | kubeedge/website | [#838](https://github.com/kubeedge/website/issues/838) | BUG: Case study cards use non-semantic clickable elements | 2026-06-27 | **Describe the bug** The case study cards on the `/case-studies` page use non-semantic clickable elements. The card title is a clickable `` and the call-to-action is an `` element without an `href`. In the rendered page, the card has no keyboard-focusable link to open the case study. |
| 11 | openSUSE/open-build-service | [#19875](https://github.com/openSUSE/open-build-service/issues/19875) | submit_request_step_spec fails on master after ReportToSCMJob retry changes | 2026-06-25 | `spec/models/workflow/step/submit_request_step_spec.rb` fails on latest `master` because the spec executes `ReportToSCMJob.perform_later` with the test environment's inline ActiveJob adapter. Once `ReportToSCMJob` retries `Octokit::Unauthorized`, the retry path tries to schedule a future job and raises `NotImplementedError` inside the step spec. |
| 12 | rage-rb/rage | [#333](https://github.com/rage-rb/rage/issues/333) | [Deferred] Async temp-file cleanup can crash later reactor-based specs | 2026-06-23 | The deferred disk backend schedules temp-file cleanup with `Iodine.run_after`, and that delayed unlink can leak into later reactor-based specs. On latest `main`, this can raise `Errno::ENOENT` from `lib/rage/deferred/backends/disk.rb` after the disk backend spec has already cleaned up its temp directory. |
| 13 | unionai-oss/pandera | [#2307](https://github.com/unionai-oss/pandera/issues/2307) | Avoid strategy truthiness checks in pandas_strategies to prevent HypothesisWarning | 2026-06-23 | `pandera/strategies/pandas_strategies.py` uses truthiness checks on Hypothesis strategies (`if strategy:`), which triggers repeated `HypothesisWarning` messages. ```bash pytest -q tests/strategies/test_strategies.py -k "strategy" --maxfail=1 ``` Observed warning snippet: ```text pandera/strategies/pandas_strategies.py:832: HypothesisWarning: bool(from_dtype(...)) is always True, did you mean to draw a value? |
| 14 | unionai-oss/pandera | [#2384](https://github.com/unionai-oss/pandera/issues/2384) | CI: do not fail matrix jobs when Codecov upload fails after tests pass | 2026-06-23 | Yes. CI matrix jobs can fail after tests pass when the Codecov upload step hits a transient network or DNS error. Observed on PR #2382: - The failing job completed the test step successfully. |
| 15 | unionai-oss/pandera | [#2368](https://github.com/unionai-oss/pandera/issues/2368) | CI: avoid misleading project coverage failures when setup jobs do not upload coverage | 2026-06-19 | **Is your feature request related to a problem? Please describe.** Yes. `codecov/project` can fail with a large apparent coverage drop when many CI matrix jobs fail before running tests and before uploading coverage. |
| 16 | tobymao/sqlglot | [#7762](https://github.com/tobymao/sqlglot/issues/7762) | Benchmark workflow fails for fork pull requests | 2026-06-18 | The benchmark workflow fails for fork-based pull requests because it fetches the PR head ref from `origin`, but fork PR branches do not exist in the upstream repository. Workflow file: - `.github/workflows/benchmark-sqlglot.yml` Current logic: - reads `headRefName` - runs `git fetch origin "$PR_HEAD"` That works for same-repo PRs, but fails for cross-repo PRs. |
| 17 | openSUSE/open-build-service | [#19814](https://github.com/openSUSE/open-build-service/issues/19814) | Intermittent ThinkingSphinx::Search::StaleIdsException in spec/models/full_text_search_spec.rb | 2026-06-18 | On the current `master` branch, `spec/models/full_text_search_spec.rb` can fail intermittently with `ThinkingSphinx::Search::StaleIdsException`. I reproduced this on a clean clone of the latest upstream `master` at `9105a3f1c6a358349893cd1b985ef5ab4dd0945e`. |
| 18 | rage-rb/rage | [#329](https://github.com/rage-rb/rage/issues/329) | [API] Ignore If-Modified-Since when If-None-Match is present | 2026-06-17 | Verified on latest `main` at `aa15f81a245fbc9c031fcb9c6e03fcffb275c7bd`. `Rage::Request#fresh?` combines `If-None-Match` and `If-Modified-Since` with a logical `&&`. When `If-None-Match` matches but `If-Modified-Since` is older than the resource, Rage renders `200 OK` and returns the response body. |
| 19 | rubocop/rubocop | [#15290](https://github.com/rubocop/rubocop/issues/15290) | Lint/ToEnumArguments false negative for braced hash passed to keyword arguments | 2026-06-17 | I have searched the existing issues and this hasn't been reported yet. I am running the latest stable version of RuboCop. False negative `Lint/ToEnumArguments` 1. |
| 20 | rubocop/rubocop | [#15291](https://github.com/rubocop/rubocop/issues/15291) | Lint/LiteralInInterpolation incorrect autocorrect for interpolated hash with quoted symbols | 2026-06-16 | I have searched the existing issues and this hasn't been reported yet. I am running the latest stable version of RuboCop. Incorrect autocorrect `Lint/LiteralInInterpolation` 1. |
| 21 | rage-rb/rage | [#324](https://github.com/rage-rb/rage/issues/324) | [API] Use weak comparison for If-None-Match validation | 2026-06-15 | `Rage::Request#fresh?` currently compares `If-None-Match` values against the response ETag using exact string equality. Rage emits weak ETags like `W/"..."`, but when the request sends the matching strong form like `"..."`, the request is treated as stale and the action still renders the body. |
| 22 | rubocop/rubocop | [#15252](https://github.com/rubocop/rubocop/issues/15252) | Lint/NumberConversion autocorrect breaks nil-safe behavior of safe navigation | 2026-06-15 | I have searched the existing issues and this hasn't been reported yet. I am running the latest stable version of RuboCop. Incorrect autocorrect `Lint/NumberConversion` I reproduced this on the latest stable RuboCop release and also on current `master`. |
| 23 | openSUSE/obs-landing | [#569](https://github.com/openSUSE/obs-landing/issues/569) | Foster collaboration series block renders broken relative links | 2026-06-15 | The shared foster collaboration series block renders several links as relative paths instead of site-root links. On current live pages, for example `/2026/05/15/label-filtering/`, links such as: - `2025-11-19-labels_and_assigments` - `2026-02-20-version-tracking-improvements` - `2026-03-03-foster-collaboration` - `2026-04-02-package-version-tracking-updates` - `2026-05-15-label-filtering` resolve under the current post URL and lead to `404` pages. |
| 24 | openSUSE/obs-landing | [#568](https://github.com/openSUSE/obs-landing/issues/568) | Content moderation series block links to non-existent 2024-08-27 post | 2026-06-15 | The shared content moderation series block links `improved the report notifications for moderators` to `/2024/08/27/content-moderation/`. That URL returns `404`. This is still rendered on current live posts, for example: - `/2024/03/06/content-moderation/` - `/2024/04/30/content-moderation/` - `/2024/08/28/content-moderation/` - `/2025/06/25/content-moderation-ui/` The `improved the report notifications for moderators` link should open the existing content moderation post instead of leading to a `404` page. |
| 25 | rage-rb/rage | [#327](https://github.com/rage-rb/rage/issues/327) | [Cookies] Match array-based cookie domains case-insensitively | 2026-06-14 | `Rage::Cookies#[]=` matches array-based cookie domains with exact string equality: ```ruby host if domain.include?(host) ``` `Rack::Request#host` preserves the request host case, so uppercase hostnames do not match lowercase configured domains. Hostnames are case-insensitive, so array-based cookie-domain matching should also be case-insensitive. |
| 26 | unionai-oss/pandera | [#2351](https://github.com/unionai-oss/pandera/issues/2351) | Resolving another pyarrow-backed pandas dtype changes ArrowString resolution on current main | 2026-06-14 | This looks like a regression related to #2228 / #2229. I reproduced it on a fresh clean clone of current `main` (`d0e5660`). ```python import pandas as pd import pyarrow from pandera.engines import pandas_engine before = pandas_engine.Engine.dtype(pd.ArrowDtype(pyarrow.string())) print("before", before.__class__.__module__, before.__class__.__name__, str(before)) pandas_engine.Engine.dtype(pd.ArrowDtype(pyarrow.list_(pyarrow.string()))) after = pandas_engine.Engine.dtype(pd.ArrowDtype(pyarrow.string())) print("after", after.__class__.__module__, after.__class__.__name__, str(after)) ``` Output on current `main`: ```text before pandera.engines.pandas_engine ArrowString arrow_string after pandera.engines.pyarrow_engine ArrowString string[pyarrow] ``` Resolving another pyarrow-backed pandas dtype should not change how `pd.ArrowDtype(pyarrow.string())` resolves. |
| 27 | unionai-oss/pandera | [#2367](https://github.com/unionai-oss/pandera/issues/2367) | CI: cache uv downloads and retry dependency installation | 2026-06-14 | **Is your feature request related to a problem? Please describe.** Yes. Large CI matrix jobs can fail before tests run when dependency downloads from PyPI or files.pythonhosted.org are interrupted. |
| 28 | openSUSE/obs-landing | [#566](https://github.com/openSUSE/obs-landing/issues/566) | Abandoned. | 2026-06-11 | Abandoned. |
| 29 | rage-rb/rage | [#320](https://github.com/rage-rb/rage/issues/320) | [Router] Exact host constraints should fall back to SERVER_NAME | 2026-06-09 | Router exact host constraints currently derive the request host from `HTTP_HOST` only. When `HTTP_HOST` is missing and the request falls back to `SERVER_NAME`, an exact host constraint like `host: "google.com"` does not match, even though the request host still resolves to `google.com`. |
| 30 | joke2k/faker | [#2355](https://github.com/joke2k/faker/issues/2355) | Add mr_IN locale with core providers | 2026-06-08 | - Faker version: 40.12.0 - OS: Windows / Linux / macOS Add full Marathi locale support as mr_IN. Faker currently has Indian language coverage in parts (for example hi_IN and gu_IN), but mr_IN is not available as a dedicated locale. Marathi is widely used in Maharashtra, the second most populous Indian state, with major industrial, tech, and financial centers like Mumbai, Pune, and Nagpur. |
| 31 | joke2k/faker | [#2366](https://github.com/joke2k/faker/issues/2366) | Flaky test: tests/test_optional.py::test_optional relies on probabilistic assertion | 2026-06-08 | - Faker version: 40.15.0 - OS: Windows 11 and WSL (Ubuntu). - Python: 3.11 The test tests/test_optional.py:10 is flaky because it expects all values of a probabilistic generator to appear in only 10 random draws. Location: tests/test_optional.py:10 The assertion requires: - {fake.optional.boolean() for _ in range(10)} == {True, False, None} This can fail randomly even when the implementation is correct. |
| 32 | rage-rb/rage | [#318](https://github.com/rage-rb/rage/issues/318) | [Cookies] Use request host fallback when resolving cookie domains | 2026-06-08 | Cookie domain resolution currently depends on `HTTP_HOST` only. When `HTTP_HOST` is missing and the request falls back to `SERVER_NAME`, `Rage::Cookies#[]=` does not resolve domains correctly: - array-based domain matching silently skips the domain attribute - `domain: :all` can raise because `DomainName(nil)` is called This is inconsistent with `Rage::Request#host`, which already falls back to `SERVER_NAME`. |
| 33 | rage-rb/rage | [#313](https://github.com/rage-rb/rage/issues/313) | [API] Reject invalid status symbols in head and render | 2026-06-07 | `head` and `render` currently accept invalid status symbols and silently set the response status to `nil`. Both methods convert symbol statuses through `Rack::Utils::SYMBOL_TO_STATUS_CODE`, but they do not validate that the symbol exists in the status table before assigning it. |
| 34 | rubocop/rubocop | [#15250](https://github.com/rubocop/rubocop/issues/15250) | Lint/LiteralInInterpolation has incorrect autocorrects for embedded quotes and escaped interpolation text | 2026-06-06 | I have searched the existing issues and this hasn't been reported yet. I am running the latest stable version of RuboCop. Incorrect autocorrect `Lint/LiteralInInterpolation` I reproduced this on the latest stable RuboCop release and also on current `master` at `21f0cb9`. |
| 35 | rage-rb/rage | [#311](https://github.com/rage-rb/rage/issues/311) | [Request] Treat IPv6 literals as non-domain hosts | 2026-06-04 | `Rage::Request#domain` currently treats IPv6 host literals as domain names instead of IP literals. With `HTTP_HOST = "[::1]:3000"`, `request.domain` returns `"[::1]"`. In the fallback path where `HTTP_HOST` is missing and `SERVER_NAME = "::1"`, `request.domain` can also raise. |
| 36 | rage-rb/rage | [#308](https://github.com/rage-rb/rage/issues/308) | [Cookies] Array domain matching fails when request host includes a port | 2026-06-03 | Cookie domain matching for array-based domains currently fails when the request host includes a port. `Rage::Cookies#[]=` compares the configured domain list against the raw `HTTP_HOST`, so a cookie setup like `domain: %w(api.test.com cookie.test.com)` does not match a request with `HTTP_HOST = "cookie.test.com:3000"`. |
| 37 | rage-rb/rage | [#306](https://github.com/rage-rb/rage/issues/306) | [Router] Exact host constraints do not match when request host includes a port | 2026-06-02 | Exact host constraints currently fail when the request host includes a port. The router derives the `host` constraint directly from `HTTP_HOST`, so a route constrained with `host: "google.com"` does not match a request with `HTTP_HOST = "google.com:3000"`. |
| 38 | rage-rb/rage | [#304](https://github.com/rage-rb/rage/issues/304) | [API] Reject malformed or empty HTTP token authorization headers | 2026-06-01 | Currently, `RageController::API#authenticate_with_http_token` accepts malformed `Authorization` headers as valid token input. The helper only checks whether the header starts with `Bearer` or `Token`, then slices the string at fixed offsets. Because of that: - `Authorization: Bearermy_token` is treated as a valid Bearer token header and yields a truncated token - `Authorization: Tokenmy_token` is treated as a valid Token header and yields a truncated token - `Authorization: Bearer ` and `Authorization: Token ` still invoke the authentication block with an empty token The method documentation says it should return `nil` when no token is found. |
| 39 | joke2k/faker | [#2365](https://github.com/joke2k/faker/issues/2365) | en_IN address provider mutates shared state in pincode_in_state(include_union_territories=True) | 2026-05-22 | The en_IN address provider mutates shared class-level state inside pincode_in_state when include_union_territories=True. Location: faker/providers/address/en_IN/__init__.py:533 Inside pincode_in_state: - codes is assigned from self.state_pincode. - include_union_territories=True then calls codes.update(self.union_territories_pincode). |
| 40 | unionai-oss/pandera | [#2309](https://github.com/unionai-oss/pandera/issues/2309) | Fix FutureWarning in test_strategies.py: DataFrame.sum(axis=None) is deprecated | 2026-05-02 | The DataFrame.sum(axis=None) call in ests/strategies/test_strategies.py (around line 778) triggers a FutureWarning in recent pandas versions because the default behavior of axis=None is changing. Running tests in ests/strategies/test_strategies.py with recent pandas: `python import pandas as pd df = pd.DataFrame({'a': [1, 2]}) df.sum(axis=None) ` FutureWarning: The behavior of DataFrame.sum with axis=None is deprecated, in a future version this will reduce over both axes and return a scalar. |
| 41 | python/tzdata | [#139](https://github.com/python/tzdata/issues/139) | update.py --source-dir crashes with AssertionError in unpack_tzdb_tarballs | 2026-04-25 | `update.py` supports `--source-dir` for local tarballs, but that code path passes only two files to `unpack_tzdb_tarballs`, which asserts four entries and crashes with `AssertionError`. ```bash cd /path/to/tzdata tmp_dir="$(mktemp -d)" curl -fsSL https://data.iana.org/time-zones/releases/tzdata2020a.tar.gz -o "$tmp_dir/tzdata2020a.tar.gz" curl -fsSL https://data.iana.org/time-zones/releases/tzcode2020a.tar.gz -o "$tmp_dir/tzcode2020a.tar.gz" python update.py --version 2020a --source-dir "$tmp_dir" --news-only ``` Observed traceback snippet: ```text File ".../update.py", line 510, in main tzdb_location = unpack_tzdb_tarballs(download_locations) File ".../update.py", line 120, in unpack_tzdb_tarballs assert len(download_locations) == 4 AssertionError ``` Using `--source-dir` crashes before unpacking because `retrieve_local_tarballs` returns two tarballs while `unpack_tzdb_tarballs` hard-asserts four paths (including `.asc` signature files). |
| 42 | python/tzdata | [#138](https://github.com/python/tzdata/issues/138) | Improve error when NEWS release header does not match expected version | 2026-04-25 | `update.py` aborts with an `AssertionError` when the release `NEWS` header does not match the expected version, which turns a recoverable release-data typo into a hard crash. ```bash tox -e update ``` Observed error snippet: ```text AssertionError ``` The release update path crashes when the `NEWS` file contains a malformed or mismatched release header, forcing a manual fix instead of giving a useful error message. |
| 43 | unionai-oss/pandera | [#2305](https://github.com/unionai-oss/pandera/issues/2305) | Wrong Issue. | 2026-04-22 | Wrong Issue. |
| 44 | unionai-oss/pandera | [#2308](https://github.com/unionai-oss/pandera/issues/2308) | Wrong Issue. | 2026-04-22 | Wrong Issue. |
| 45 | unionai-oss/pandera | [#2306](https://github.com/unionai-oss/pandera/issues/2306) | Wrong Issue. | 2026-04-22 | Wrong Issue. |
| 46 | tobymao/sqlglot | [#7478](https://github.com/tobymao/sqlglot/issues/7478) | Enhancement: richer typed handling for BigQuery AI.* scalar functions | 2026-04-15 | `AI.EMBED`, `AI.SIMILARITY`, and `AI.GENERATE` in BigQuery currently parse and round trip correctly, but they are represented through the generic function nodes rather than the richer typed AST nodes. ```python from sqlglot import parse_one from sqlglot import expressions as exp for sql in ( 'SELECT AI.EMBED("hello")', 'SELECT AI.SIMILARITY("a", "b")', 'SELECT AI.GENERATE("Write a haiku")', ): expr = parse_one(sql, dialect="bigquery") node = expr.expressions[0] fn = node.expression if isinstance(node, exp.Dot) else None print(sql) print("top:", type(node).__name__) print("fn:", type(fn).__name__ if fn else None) print("roundtrip:", expr.sql(dialect="bigquery")) ``` - Parse and round trip work for common documented scalar forms. |
| 47 | unionai-oss/pandera | [#2280](https://github.com/unionai-oss/pandera/issues/2280) | FutureWarning: DataFrame.applymap deprecation in pandas parser element-wise path | 2026-04-12 | Running pandas parser validation with element wise dataframe parsing emits: FutureWarning: DataFrame.applymap has been deprecated. Use DataFrame.map instead. This appears because the pandas parser backend uses DataFrame.applymap in the pre-pandas-3 branch. |
| 48 | tobymao/sqlglot | [#7462](https://github.com/tobymao/sqlglot/issues/7462) | BigQuery parser fails on MODEL-first AI or ML GENERATE function signatures | 2026-04-07 | `AI/ML.GENERATE*` `MODEL`-first function syntax in BigQuery currently fails with parser errors when using the documented signatures. The issue is reproducible on the latest `main` branch of SQLGlot (verified at commit `2367eeb3`). ```python from sqlglot import parse_one sql = "SELECT * FROM ML.GENERATE_TEXT(MODEL `mydataset.gemini_model`, TABLE `mydataset.prompt_table`, STRUCT(0.15 AS temperature))" parse_one(sql, dialect="bigquery") ``` Raises: ```text sqlglot.errors.ParseError: Expecting ). |
| 49 | Delgan/loguru | [#1461](https://github.com/Delgan/loguru/issues/1461) | Multithreaded Logging Slowdown from Thread-Safe Sink Synchronization | 2026-04-05 | Earlier discussed in the issue: #1437 I ran the scenario, and wrote this. The slowdown is reproducible in multithreaded logging workloads. The magnitude varies by runtime mode, logging frequency, and queueing mode. |
| 50 | tobymao/sqlglot | [#7456](https://github.com/tobymao/sqlglot/issues/7456) | BigQuery: AI.FORECAST TVF syntax fails to parse | 2026-04-05 | `AI.FORECAST` table-valued function syntax in BigQuery currently fails with a parser error when using documented named arguments. The issue is reproducible in the latest main branch of SQLGlot. ```python from sqlglot import parse_one sql = 'SELECT * FROM AI.FORECAST(TABLE citibike_trips, data_col => "num_trips", timestamp_col => "date", horizon => 30)' parse_one(sql, dialect="bigquery") ``` Raises: ```text sqlglot.errors.ParseError: Invalid expression / Unexpected token. |


### Triaged Issues

Issues opened by others that were closed after you commented or contributed to triaging them.


| # | Repository | Issue | Title | Closed Date | Description |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | scarpe-team/scarpe | [#453](https://github.com/scarpe-team/scarpe/issues/453) | We should pass filename and line to eval for ShoesSpec (etc) testing | 2026-06-29 | When Ruby does an eval (or instance_eval, etc.) you can pass a filename and line number in so that it reports them better in backtraces. In CatsCradle and ShoesSpec testing in the scarpe gem, and in ShoesSpec testing in the shoes-spec gem, we don't consistently do that. |
| 2 | unionai-oss/pandera | [#2390](https://github.com/unionai-oss/pandera/issues/2390) | xarray backend: `regex=True` on `DataVar` is matched literally, not as a regex | 2026-06-29 | In the **xarray backend**, passing `regex=True` to a `DataVar` in a `DatasetSchema` does not work. The `data_vars` key is treated as a *literal* variable name rather than being compiled and matched as a regular expression. As a result, a pattern that should match several data variables only ever matches a variable literally named after the (metacharacter-stripped) pattern. |
| 3 | openSUSE/open-build-service | [#12341](https://github.com/openSUSE/open-build-service/issues/12341) | Wrong link on workflow run details for failed push events | 2026-06-24 | The details of a workflow run usually contain a link to the PR (for pull request events) or a link to the commit (for push events). However, when the push event fails, we are displaying a link to the current page. In that case, the payload's key-value pair we try to get the URL from looks like this `"head_commit": null`. |
| 4 | openSUSE/osem | [#3143](https://github.com/openSUSE/osem/issues/3143) | Access denied when organizer adds commercial | 2026-06-18 | Bug Report Feature Request > You are not authorized to access this page. ``` Started POST "/conferences/example/program/proposals/1/commercials" Processing by CommercialsController#create as HTML Access denied on create # Redirected to http://localhost:3000/ Completed 302 Found ``` Successful addition of a commercial 1. |
| 5 | duckdb/duckdb | [#21757](https://github.com/duckdb/duckdb/issues/21757) | SELECT DISTINCT in CTE with LEFT JOIN on empty table intermittently returns wrong results | 2026-06-18 | SELECT DISTINCT inside a CTE that LEFT JOINs an empty table intermittently returns incorrect (missing) rows when the CTE is subsequently joined. The issue also persists, but is less prevalent when replaced with GROUP BY. ```python import duckdb from uuid import uuid4 failures = 0 for i in range(10000): ids = [str(uuid4()) for _ in range(5)] with duckdb.connect() as conn: conn.execute("CREATE TABLE items (id VARCHAR, category VARCHAR)") conn.executemany("INSERT INTO items VALUES (?, ?)", [ (ids[0], "A"), (ids[1], "A"), (ids[2], "B"), (ids[3], "B"), (ids[4], "A"), ]) conn.execute("CREATE TABLE mappings (id VARCHAR, parent_id VARCHAR)") conn.execute("INSERT INTO mappings VALUES (?, ?)", [ids[3], ids[4]]) conn.execute("CREATE TABLE groups (id VARCHAR, group_id VARCHAR)") # groups is intentionally empty result = conn.execute(""" WITH resolved AS ( SELECT DISTINCT i.id, COALESCE(g.group_id, m.parent_id, i.id) AS resolved_id FROM items i LEFT JOIN mappings m ON i.id = m.id LEFT JOIN groups g ON COALESCE(m.parent_id, i.id) = g.id ) SELECT DISTINCT r.resolved_id FROM items i JOIN resolved r ON i.id = r.id WHERE i.category = ? |
| 6 | unionai-oss/pandera | [#1357](https://github.com/unionai-oss/pandera/issues/1357) | Include drop_invalid_rows attribute in deserialization from_json() | 2026-06-18 | **Is your feature request related to a problem? Please describe.** drop_invalid_rows attribute is not included when DataFrameSchema is deserialized when using from_json **Describe the solution you'd like** Include drop_invalid_rows attribute to [pandera.io.deserialize_schema()](https://github.com/unionai-oss/pandera/blob/main/pandera/io/pandas_io.py#L239) for DataFrameSchema **Describe alternatives you've considered** Currently manually setting drop_invalid_rows to True even though json schema specifies this. |
| 7 | unionai-oss/pandera | [#1339](https://github.com/unionai-oss/pandera/issues/1339) | 'drop_invalid_rows' always False with from_json() and to_json() | 2026-06-18 | **Describe the bug** 'drop_invalid_rows: false' argument at a DataFrameSchema level gets set to False using .from_json(). Creating json from .py with 'drop_invalid_rows=True' does not work either. I have checked that this issue has not already been reported. |
| 8 | rubocop/rubocop | [#14364](https://github.com/rubocop/rubocop/issues/14364) | Cannot modify the configuration of a local custom cop | 2026-06-15 | I defined a custom cop in `lib/rubocop/cop/foo/bar.rb` and attempted to change its configuration in `.rubocop.yml`. For example, if I set `SafeAutoCorrect: false` for this custom cop, I expect `.rubocop_todo.yml` to include a comment like "This cop supports **unsafe** autocorrection" for this cop. |
| 9 | plotly/plotly.py | [#5583](https://github.com/plotly/plotly.py/issues/5583) | Non idiomatic method y_axis | 2026-06-15 | In Rust, unlike in JavaScript, we are accustomed to using numbers to represent numbers, strings for strings, and Enums for enumerable data. In your code, I see a mix of the correct approach alongside some unidiomatic and incorrect patterns. **Example of the correct approach** The method `Layout::bar_mode(self, value: BarMode)` is a great example. |
| 10 | unionai-oss/pandera | [#1309](https://github.com/unionai-oss/pandera/issues/1309) | Handling columns with multiple timezones | 2026-06-14 | Validating a column of time zone aware datetimes fail when there are multiple time zones. I have checked that this issue has not already been reported. I have confirmed this bug exists on the latest version of pandera. |
| 11 | openSUSE/obs-landing | [#170](https://github.com/openSUSE/obs-landing/issues/170) | Make <h> consistent in blog post | 2026-06-10 | It is really weird that `h2` is bigger than `h1` in the blog posts. :scream: |
| 12 | tobymao/sqlglot | [#7548](https://github.com/tobymao/sqlglot/issues/7548) | ClickHouse dialect: SHOW and EXPLAIN parse as Command (fallback for unknown syntax) | 2026-05-22 | In the `clickhouse` dialect, `SHOW` and `EXPLAIN` statements parse to `exp.Command` — sqlglot's catch-all for syntax it can't recognise — rather than to dedicated AST nodes. This makes them indistinguishable from genuinely-unknown statements like `SYSTEM`, `RENAME`, `ATTACH`, `OPTIMIZE`, etc., which also fall back to `Command`. |
| 13 | unionai-oss/pandera | [#2294](https://github.com/unionai-oss/pandera/issues/2294) | ibis validation fails checks if column all nulls | 2026-05-22 | **Describe the bug** Pandera validation checks fail when a nullable column is all null. Thi I have checked that this issue has not already been reported. I have confirmed this bug exists on the latest version of pandera. |
| 14 | unionai-oss/pandera | [#2110](https://github.com/unionai-oss/pandera/issues/2110) | Pandera cannot handle metadata through Annotated types | 2026-05-21 | **Describe the bug** Field metdata defined in the Schema class using Annotated type and pa.Field(…) were not being applied correctly to the schema fields in the metadata, while using just pa.Field(…) correctly applies. I have checked that this issue has not already been reported. |
| 15 | unionai-oss/pandera | [#2297](https://github.com/unionai-oss/pandera/issues/2297) | Mypy type hinting error for direct column type annotations with Polars | 2026-05-21 | **Describe the bug** A clear and concise description of what the bug is. I have checked that this issue has not already been reported. I have confirmed this bug exists on the latest version of pandera. |
| 16 | unionai-oss/pandera | [#2143](https://github.com/unionai-oss/pandera/issues/2143) | Include error output in DataFrameSchema.to_json() / to_yaml() + Read error from yaml / json files | 2026-05-10 | **Is your feature request related to a problem? Please describe.** Currently for output DataFrameSchema to json or yaml files, error message is not included. Also it is not possible to include error message through json or yaml file. |
| 17 | sqlfluff/sqlfluff | [#7723](https://github.com/sqlfluff/sqlfluff/issues/7723) | Support CONTAINSTABLE for T-SQL | 2026-05-05 | I searched the [issues](https://github.com/sqlfluff/sqlfluff/issues) and found no similar issues. SQLFluff fails to parse a `CONTAINSTABLE` statement. SQLFluff using T-SQL dialect should be able to parse a `CONTAINSTABLE` statement. |
| 18 | dlt-hub/dlt | [#3797](https://github.com/dlt-hub/dlt/issues/3797) | ClickHouse merge creates delete temp table with MergeTree, causing duplicate PK rows on ReplicatedMergeTree clusters | 2026-05-04 | 1.24.0 When using dlt ClickHouse destination with destination tables configured as `ReplicatedMergeTree` and `write_disposition="merge"`, incremental runs may produce duplicate primary-key rows. After investigation, merge followup jobs create the delete temp table with a hardcoded `MergeTree` engine. |
| 19 | unionai-oss/pandera | [#1649](https://github.com/unionai-oss/pandera/issues/1649) | Optional import hypotheses doesn't install hypothesis | 2026-05-02 | https://pandera.readthedocs.io/en/stable/data_synthesis_strategies.html#usage-in-unit-tests https://pandera.readthedocs.io/en/stable/index.html#extras When reading about how to create example dataframes from schemas on [this page](https://pandera.readthedocs.io/en/stable/data_synthesis_strategies.html#usage-in-unit-tests) it mentions that you need hypothesis library. |
| 20 | dlt-hub/dlt | [#3806](https://github.com/dlt-hub/dlt/issues/3806) | ClickHouse: ORDER BY tuple() on internal metadata tables causes TBs/day of wasted read I/O at scale | 2026-04-28 | 1.24.0 The three dlt internal metadata tables (`_dlt_version`, `_dlt_pipeline_state`, `_dlt_loads`) are created on ClickHouse with `MergeTree ORDER BY tuple()` — no primary key, no ordering, no deduplication. Combined with dlt's append-only write pattern (rows are inserted but never deleted), these tables grow linearly with the number of pipeline runs. |
| 21 | koaning/drawdata | [#34](https://github.com/koaning/drawdata/issues/34) | ReferenceError breaks widget initialization in non-Jupyter environments | 2026-04-23 | `drawdata.ScatterWidget()` throws `Uncaught ReferenceError: Cannot access 'circle_brush' before initialization` when rendered in frameworks that use the anywidget ESM spec directly (e.g., Panel's upcoming `AnyWidget` pane, ). This error breaks the widget's `model.on("change:...")` event handlers, preventing bidirectional sync between the frontend and Python. |
| 22 | joke2k/faker | [#2361](https://github.com/joke2k/faker/issues/2361) | UserWarning: The Provider provider does not have a 'banks' attribute. Consider contributing to the project and  adding a 'banks' tuple to enable bank name generation. | 2026-04-17 | OS: Windows 11 Lang: Traditional Chinese Python: Python 3.12.4 `C:\> chcp` Active code page: 950 `C:\> pip install faker` `C:\> python -m faker 1>nul: ` : ``` C:\Users\User\anaconda3\Lib\site-packages\faker\documentor.py:116: UserWarning: The Provider provider does not have a 'banks' attribute. |
| 23 | tobymao/sqlglot | [#7480](https://github.com/tobymao/sqlglot/issues/7480) | BigQuery: LOAD DATA ... FROM FILES(...) raises ParseError: Required keyword 'inpath' missing | 2026-04-15 | SQLglot fails to parse this kind of queries ([this query is taken from BigQuery documentation](https://docs.cloud.google.com/bigquery/docs/reference/standard-sql/load-statements#overwrite_an_existing_table)): ```sql LOAD DATA OVERWRITE mydataset.table1 FROM FILES( format='AVRO', uris = ['gs://bucket/path/file.avro'] ) ``` Error message: ```bash ParseError: Required keyword: 'inpath' missing for . |
| 24 | tobymao/sqlglot | [#7483](https://github.com/tobymao/sqlglot/issues/7483) | BigQuery: FOR...IN loop not supported? | 2026-04-14 | SQLglot fails to parse a valid BQ query: ```python query = """ FOR tn IN (SELECT '101' AS number) DO EXECUTE IMMEDIATE FORMAT("SELECT %s", tn.number); END FOR """ sqlglot.parse_one(query, dialect="bigquery") ``` Error: ```bash ParseError: Invalid expression / Unexpected token. |
| 25 | unionai-oss/pandera | [#1314](https://github.com/unionai-oss/pandera/issues/1314) | Pyspark module - str_length check not implemented. | 2026-04-14 | When using the `pandera.pyspark` module, validation of a `DataFrameSchema` that uses `Check.str_length()` in a column level check generates `NotImplementedError`. I have checked that this issue has not already been reported. I have confirmed this bug exists on the latest version of pandera. |
| 26 | unionai-oss/pandera | [#1311](https://github.com/unionai-oss/pandera/issues/1311) | str_length function not working in pa.Field for PySpark | 2026-04-14 | **Describe the bug** When trying to use the str_length function in pa.Field to validate the length of a string, we get a NotImplementedError every time. I tried passing arguments in different ways, as in the screenshot, and in the form of str_length(1, 2), both options give the same error I have checked that this issue has not already been reported. |
| 27 | unionai-oss/pandera | [#2152](https://github.com/unionai-oss/pandera/issues/2152) | `pa.Column` mypy typing should accept `pl.Decimal` | 2026-04-13 | **Describe the bug** As title. I have checked that this issue has not already been reported. I have confirmed this bug exists on the latest version of pandera. |
| 28 | unionai-oss/pandera | [#2098](https://github.com/unionai-oss/pandera/issues/2098) | Pylance: "errors" is not exported from module "pandera.pandas" | 2026-04-13 | **Describe the bug** Similar to issue #1726, "errors" also not included in `__all__` in pandas module. Adding "errors" solves the Pylance problem. I have checked that this issue has not already been reported. |
| 29 | pola-rs/polars | [#27106](https://github.com/pola-rs/polars/issues/27106) | NotImplementedError when using scan_iceberg with `is_in([])` filter | 2026-04-13 | I have checked that this issue has not already been reported. I have confirmed this bug exists on the [latest version](https://pypi.org/project/polars/) of Polars. ```python import tempfile from pathlib import Path import polars as pl import pyarrow as pa from pyiceberg.catalog.sql import SqlCatalog from pyiceberg.schema import Schema from pyiceberg.types import LongType, NestedField temp_dir = tempfile.mkdtemp() catalog = SqlCatalog( "test", uri=f"sqlite:///{Path(temp_dir)}/catalog.db", warehouse=f"file://{Path(temp_dir)}/warehouse", ) catalog.create_namespace("default") table = catalog.create_table( "default.my_table", schema=Schema( NestedField(1, "my_column", LongType(), required=False), ), ) table.append( pa.table( {"my_column": [1, 2, 3, 4, 5]}, schema=pa.schema([("my_column", pa.int64())]), ) ) lf = pl.scan_iceberg(catalog.load_table("default.my_table")) lf.filter(pl.col("my_column").is_in([])).collect() ``` ```shell async thread count: 8 blocking thread count: 512 python dataset: convert from arrow schema expand_datasets(): python[IcebergScanResolver]: limit: None, project: all IcebergScanResolver: to_dataset_scan(): snapshot ID: None, limit: None, projection: None, filter_columns: ['my_column'], pyarrow_predicate: Some(), iceberg_table_filter: Some(), self.use_metadata_statistics: True [SENSITIVE]: IcebergScanResolver: to_dataset_scan(): pyarrow_predicate = 'pa.compute.scalar(False)', iceberg_table_filter = False IcebergScanResolver: to_dataset_scan(): tbl.metadata.current_snapshot_id: 5723962245912935644 IcebergStatisticsLoader: field.name = 'my_column', field.field_id = 1, field.field_type = LongType(), field_all_types = {LongType()}, field_polars_dtype = Int64, _load_from_bytes_impl = 'LoadInt64FromBytes' IcebergScanResolver: to_dataset_scan(): begin path expansion Traceback (most recent call last): File "/mnt/home/kevin.jiao/repos/core/claude_worktrees/polars-repro/repro.py", line 34, in lf.filter(pl.col("my_column").is_in([])).collect() ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~^^ File "/mnt/home/kevin.jiao/repos/core/claude_worktrees/polars-repro/.venv/lib/python3.13/site-packages/polars/_utils/deprecation.py", line 97, in wrapper return function(*args, **kwargs) File "/mnt/home/kevin.jiao/repos/core/claude_worktrees/polars-repro/.venv/lib/python3.13/site-packages/polars/lazyframe/opt_flags.py", line 326, in wrapper return function(*args, **kwargs) File "/mnt/home/kevin.jiao/repos/core/claude_worktrees/polars-repro/.venv/lib/python3.13/site-packages/polars/lazyframe/frame.py", line 2464, in collect return wrap_df(ldf.collect(engine, callback)) ~~~~~~~~~~~^^^^^^^^^^^^^^^^^^ File "/mnt/home/kevin.jiao/repos/core/claude_worktrees/polars-repro/.venv/lib/python3.13/site-packages/polars/io/iceberg/_dataset.py", line 235, in to_dataset_scan scan_data := self._to_dataset_scan_impl( ~~~~~~~~~~~~~~~~~~~~~~~~~~^ existing_resolved_version_key=existing_resolved_version_key, ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ ...... |
| 30 | unionai-oss/pandera | [#2129](https://github.com/unionai-oss/pandera/issues/2129) | Parser not invoked when `coerce = True` | 2026-04-12 | **Describe the bug** A clear and concise description of what the bug is. - [ x] I have checked that this issue has not already been reported. - [ x] I have confirmed this bug exists on the latest version of pandera. |
| 31 | unionai-oss/pandera | [#2182](https://github.com/unionai-oss/pandera/issues/2182) | Multiindexed columns (schemas) are not serialisable | 2026-04-10 | Pandas schemas for multi-indexed columns cannot be serialised to JSON ``` import pandas as pd import pandera.pandas as pa schema = pa.DataFrameSchema({ ('level1', 'col_a'): pa.Column(float), ('level1', 'col_b'): pa.Column(float), ('level2', 'col_c'): pa.Column(int), }) df = pd.DataFrame({ ('level1', 'col_a'): [1.0, 2.0, 3.0], ('level1', 'col_b'): [4.0, 5.0, 6.0], ('level2', 'col_c'): [7, 8, 9], }) print('DataFrame columns:') print(df.columns) validated = schema.validate(df) json_str = schema.to_json() ``` Failes with ``` TypeError: keys must be str, int, float, bool or None, not tuple ``` |
| 32 | unionai-oss/pandera | [#2188](https://github.com/unionai-oss/pandera/issues/2188) | Ibis table validation failed cases does not include correct index of the row. | 2026-04-09 | New to pandera, trying to validate Ibis table (default duckdb backend) with pandera, validation works ok, but the `failed_cases` `index` column does not reflect real row index, i have a jsonl file, ibis duckdb backend keeps the order of rows in the table according to documentaion. |
| 33 | unionai-oss/pandera | [#2215](https://github.com/unionai-oss/pandera/issues/2215) | Metadata on index fields is not persisted | 2026-04-09 | **Describe the bug** The metadata dictionary that can be provided to the [pandera.Field](https://pandera.readthedocs.io/en/latest/reference/generated/pandera.api.dataframe.model_components.Field.html) model component is not persisted for fields annotated as `Index`, while it is for fields annotated as `Series`. |
| 34 | unionai-oss/pandera | [#2183](https://github.com/unionai-oss/pandera/issues/2183) | Nullable pa.Date field with add_missing_columns=True fails validation when column is auto-added with pd.NaT default | 2026-04-08 | When using `add_missing_columns = True` with a `pa.Date` field that has `nullable=True` and a default value of `pd.NaT`, Pandera raises a `SchemaError` claiming the series doesn't have type `date`, even though the field is properly configured to accept null values. |
| 35 | unionai-oss/pandera | [#2176](https://github.com/unionai-oss/pandera/issues/2176) | Inconsistent behaviour nullable bool and int typed columns. | 2026-04-08 | **Describe the bug** A clear and concise description of what the bug is. I have checked that this issue has not already been reported. I have confirmed this bug exists on the latest version of pandera. |
| 36 | unionai-oss/pandera | [#2219](https://github.com/unionai-oss/pandera/issues/2219) | `str_length_strategy` does not implement new `exact_length` argument | 2026-04-08 | **Describe the bug** `str_length_strategy` does not implement new `exact_length` argument. In #2198, pandera added an `exact_length` argument to the `str_length` check. This change was not also added to the generation strategy. |
| 37 | pydantic/pydantic-extra-types | [#366](https://github.com/pydantic/pydantic-extra-types/issues/366) | feature request: JSON Schema | 2026-04-05 | relates to https://github.com/pydantic/pydantic/discussions/5135 Would be cool to have a type representing a JSON Schema (maybe a bit meta 😵‍💫 ) This type could take a str or dict, and be represented as a dict with a strict type matching a valid JSON Schema. |
| 38 | pydantic/pydantic-extra-types | [#198](https://github.com/pydantic/pydantic-extra-types/issues/198) | Add ISIN type to types | 2026-04-05 | I suggest adding a validation for ISINs to the types. I have the code ready with no extra dependencies required and will create a PR should you decide to add it to this repo. |
| 39 | pydantic/pydantic-extra-types | [#364](https://github.com/pydantic/pydantic-extra-types/issues/364) | [pendulum] Missing wrapper for Interval type | 2026-04-05 | In the current set of pendulum types, Interval is missing. Following the approach for the other types, support for it could be achieved with the following code (please note that timedelta_schema was used as core_schema, but something different or ad-hoc could be needed here. |
| 40 | Delgan/loguru | [#1417](https://github.com/Delgan/loguru/issues/1417) | FYI: Another malicious clone in Github & PyPI | 2026-04-05 | Hi! Just letting you know, there is another malicious clone, named `logguru`, on PyPI and Github. PyPI: hxxps://pypi[.]org/project/logguru GitHub: hxxps://github[.]com/Delgann/loguru If used, it will download malware: The downloaded binary seems not to be detected by AVs yet (https://www.virustotal.com/gui/file/e3252fc08959fbb3e749f74eba1720efca62982e7017e519f54cb9c30480ee8f/detection) but acts as a stealer and attempts to collect browser data: https://tria.ge/251129-mp6cgsgk5v/behavioral1 I hope both projects will be gone soon, but I'm leaving the message here as a precaution for people. |
| 41 | pola-rs/polars | [#27093](https://github.com/pola-rs/polars/issues/27093) | Series.__setitem__ silently ignores integer Series index with non-UInt32/UInt64 dtype | 2026-04-05 | I have checked that this issue has not already been reported. I have confirmed this bug exists on the [latest version](https://pypi.org/project/polars/) of Polars. ```python import polars as pl s = pl.Series("a", [1, 2, 3]) idx = pl.Series([0, 2]) # Int64 by default s[idx] = 99 print(s.to_list()) # [1, 2, 3] — unchanged, no error ``` _No error output — that's the problem._ `Series.__setitem__` with a Series key only handles `Boolean`, `UInt32`, and `UInt64` dtypes. |
| 42 | unionai-oss/pandera | [#2205](https://github.com/unionai-oss/pandera/issues/2205) | Pandera MultiIndex tz-aware level loses timezone during validation when full materialization path is used | 2026-04-04 | **Describe the bug** When validating a tz‑aware MultiIndex level, Pandera’s full‑materialization path converts the level via .values, which drops timezone info. This leads to a dtype mismatch error: expected datetime64[ns, ] but got datetime64[ns], even though the MultiIndex level is tz‑aware. |
| 43 | unionai-oss/pandera | [#2211](https://github.com/unionai-oss/pandera/issues/2211) | Allow newer union type annotations when using DataFrameModels | 2026-04-03 | **Is your feature request related to a problem? Please describe.** The following is acceptable to mark fields as optional without adding spec to an inner `Config` ```python from typing import Optional import pandera.pandas as pdm from pandera.typing import pandas as pdm_types class InputMetadataModel(pdm.DataFrameModel): size: pdm_types.Series[int] label: pdm_types.Series[str] note: Optional[pdm_types.Series[str]] ``` But it will flag against [UP007](https://docs.astral.sh/ruff/rules/non-pep604-annotation-union/#non-pep604-annotation-union-up007). |
| 44 | unionai-oss/pandera | [#2247](https://github.com/unionai-oss/pandera/issues/2247) | The Pandera DataFrame constructor fails on a dict with empty lists, because series are assumed to be float64 if not coercing | 2026-04-02 | **Describe the bug** When constructing a validated dataframe with `pandera.typing.DataFrame` on a dict of empty lists, all fields are assumed to be `float64` and therefore validation fails, unless `coerce=True`. - [Y] I have checked that this issue has not already been reported. |



*Last updated: 2026-07-03 06:16:56 UTC*